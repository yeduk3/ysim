# Current Work — Render-State Decoupling Slice (feat/render-state-decoupling)

- File in flight: none — slice complete; ready for Estimator after the user's manual visual-regression check.
- How far: all 8 PLAN.md todos done. `MeshGL<CPU>` lifted from `src/main.cpp` into `include/MeshGL.hpp`. New `include/MeshRenderState.hpp` owns per-mesh GL state keyed by `mesh.id` with lazy `getOrCreate(mesh)` construction. `GeneralMesh::meshGL` field, move-ctor entry, and `mesh.initialize()` GL line removed. `Simulator` holds a `renderState` member; `draw`, `update`'s mesh-upload path, and `initialize`'s post-`pack` clear all route through it. The dead CPU `ExplicitSystem` path is now a stub `(void)sceneObjects;` (was an uninstantiated `mesh.meshGL.updateBuffer` reference).
- What's tested: `./scripts/verify.sh` clean. 11 + 9 doctest cases / 1279 assertions across both binaries (unchanged from prior slice — this slice is infrastructure). `ysim` GUI binary builds. **Manual visual-regression check is the user's gate** — render output must be pixel-identical: pre-loaded cloth + Human + ground draw, `Create > Sphere/Cube` modals work and the new mesh appears, `Save Scene` / `Load Scene` cycles render the loaded scene.
- What's next: user runs `./build/src/ysim`, sanity-checks the existing scene + Create + Load look identical to before. Then Estimator review. After this lands, the **test-harness slice** (Metal-backed sim fixture closing parked BDD-009/011/012/015 sim-step clauses) is unblocked.
