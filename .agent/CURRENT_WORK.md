# Current Work — BDD-006 BLOCK fix-turn complete (`feat/bdd-006-behavior-assignment-ui` via worktree `fixturn-bdd-006`)

- File in flight: none — fix-turn complete. **57/57 self-test PASS** deterministic across 5 runs. Doctest 159/159 + 1120/1120 SUCCESS via `verify-light.sh`.
- How far: closes Estimator turn-32 BLOCK + WARNING via option (b) for BLOCK (load-side accept) and cache-sync lambda for WARNING. All PLAN todos done.
  - **scene_format.hpp reserved-list edits** — `isReservedBehavior` narrowed from `{Rigid, Elastic, Fluid, Generator}` to `{Elastic, Fluid, Generator}`; `isKnownBehavior` widened to include `"Rigid"`. After D-036 Rigid HAS runtime tag-set support; "reserved-not-shipped" intent was "no runtime support" — the membership was stale.
  - **`Simulator::loadScene` Rigid branch** (`src/main.cpp:~5505`) — added `else if (o.behavior.type == "Rigid")` between FastGridCloth and the fall-through; sets `btype = BehaviorType::Rigid; bparams = FloatBehaviorParams<PR>{}` (FloatBehaviorParams placeholder satisfies the variant since BehaviorParams has no Rigid alternative).
  - **`Simulator::changeBehavior` cache-sync lambda** (`src/main.cpp:~4920`) — added `syncBroadPhaseCaches(BehaviorType bt)` inside the method that writes `collisionPipeline.broadPhase.objTrees[idx].objBehavior = bt` AND `shBroadPhase.meshBehaviors[idx] = (uint32_t)bt` when caches are allocated (gated on `objTrees.size()`, `meshBehaviors.ptr`, `meshBehaviors.size`). Called at the tail of each accept case (Float / TriangularCloth / FastGridCloth / Rigid).
  - **`test/scene_io_test.cpp` BDD-016 swap** — `"behavior": {"type":"Rigid", ...}` literal changed to `"type":"Elastic"`; error-message find check from `"Rigid"` to `"Elastic"`. Test intent (reserved-not-shipped rejection on load) preserved.
  - **Block 29 clauses 5 + 6** added to `src/main.cpp::runSelfTest`:
    1. **Clause 5 — Rigid save→load round-trip**: addCube → changeBehavior(0, Rigid) → snapshot via `sim.saveScene(/tmp/...)` → `sim.loadScene` → `sim.initialize()` + `sim.applyPendingMaterials()` → assert post-load `mesh.behaviorType == Rigid`. Pass label: `BDD-006 / Rigid round-trip through saveScene/loadScene preserves the Rigid tag (D-036 addendum, turn-32 fix-turn)`.
    2. **Clause 6 — changeBehavior cache-sync**: addCube + initialize + 1 frame pump (populate caches) → pre-state assert `objTrees[0].objBehavior == Float` (force-rebuilt by D-026 skip-rebuild logic; meshBehaviors pre-state intentionally NOT asserted because `rebuildMeshKinds` size-match short-circuit leaks residuals across resetScene) → `changeBehavior(0, TriangularCloth)` → immediately assert `objTrees[0].objBehavior == TriangularCloth` AND `meshBehaviors[0] == (uint32_t)TriangularCloth` BEFORE any further `sim.update()`. Pass label: `BDD-006 / changeBehavior immediately syncs broad-phase cached behavior arrays (D-036 addendum, turn-32 fix-turn)`.
  - **`docs/DECISIONS.md` D-036 addendum appended** — paragraph documenting (i) persistence symmetry shape + rationale (option b over a/c), (ii) cache-sync invariant + lambda body, (iii) 4 bug-probe shapes, (iv) BDD-006-RIGID-DISPATCH-PARKED narrowing.
  - **`docs/TEST_MATRIX.md` BDD-006 row** — test-address column appended with the 2 new pass labels + persistence-symmetry summary; bug-probe count updated. Status stays `pass`.
  - **`docs/roles/PLANNER.md` Standing constraints** — BDD-006-RIGID-DISPATCH-PARKED entry narrowed per the Spec substitution section (persistence done; only integrator dispatch parked).
- What's tested:
  - 57/57 self-test PASS deterministic across 5 consecutive runs from `build/` (55 prior + 2 fix-turn clauses).
  - Block 29 + Blocks 1–28 all green.
  - Doctest 159/159 + 1120/1120 SUCCESS via verify-light.sh (BDD-016 stays green with Elastic substitution).
- **Bug-probes (all LOAD-BEARING):**
  - **(a) Restore `"Rigid"` to `isReservedBehavior`**: Clause 5 FAILs with `loadErr=behavior 'Rigid' not available in this build (objects[0])` — exactly the turn-32 BLOCK signature. Restored.
  - **(b) Disable `else if (o.behavior.type == "Rigid")` branch in loadScene**: Clause 5 reports `loadOk=1 postTagOk=0` (load succeeds at JSON layer; tag silently demotes to Float at the runtime layer). Restored.
  - **(c) Comment out `syncBroadPhaseCaches(TriangularCloth)` in changeBehavior's Cloth case**: Clause 6 FAILs with `postObjBehaviorOk=0 postMeshBehaviorsOk=0` — both caches stay stale. Restored.
  - **(d) Restore `"Rigid"` literal in BDD-016 doctest without reverting (a)**: doctest case FAILs (3 assertions). `r.ok` is now true (Rigid loads cleanly), `r.error.message` is empty, so both `.find("Elastic")` and `.find("not available")` checks return `npos`. Confirms the test-update is load-bearing. Restored.
- **Manual GUI test (user-driven, post-slice).** Before `/codex:rescue`: launch `./build/src/ysim`. (1) Create cube. (2) Inspector → Behavior → Rigid. (3) Save scene to disk. (4) Load scene back. (5) Confirm the cube's behavior tag is still Rigid in the inspector after reload. (6) Bonus: switch through Float→Cloth on a freshly-loaded scene; collision filter reacts immediately (no one-frame lag).
- Non-goals respected: NO B-3 work (Rigid integrator dispatch parked); NO new D-NNN; NO new CM-NNN; NO change to `Simulator::changeBehavior`'s switch shape or rejection set; NO loosening of reserved-not-shipped widget hiding; NO change to params shape for Rigid on disk; NO Block-26 extension; NO new BDD/FR. Existing 4 clauses + all other blocks + scene_format helpers + doctest cases byte-identical.
- What's next: Estimator turn 33 review via `Agent(codex:codex-rescue)`. Expected verdict: NOTE or WARNING. Possible items:
  - (i) D-036 turn-32 fix-turn addendum prose may include too much detail for a CHANGELOG-style entry — could trim. NOTE-able.
  - (ii) Clause 6's pre-state assertion deliberately doesn't check `meshBehaviors` (rebuildMeshKinds size-match short-circuit); the comment in the source explains this. Estimator may flag as "incomplete coverage" — informational; the post-state check is what closes the WARNING.
  - (iii) BDD-006-RIGID-DISPATCH-PARKED standing constraint narrows this turn — first appearance of the narrowed text. Estimator should see scope reduction.
  - (iv) Worktree-isolation pathway (slice's uncommitted state was patched into `fixturn-bdd-006` worktree before fix-turn could plan against it) is a process detail; close-out will need to merge the worktree branch back into `feat/bdd-006-behavior-assignment-ui` in the primary checkout — the user's working copy still carries the pre-fix-turn slice WIP that needs to be unstaged before the merge.
