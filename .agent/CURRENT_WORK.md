# Current Work — B-1 RigidPhysicsBackend contract + Null impl complete (`feat/b-1-rigid-physics-backend-contract`)

- File in flight: none — slice complete. **59/59 self-test PASS** deterministic across 5 runs from `build/`. Doctest 159/159 + 1120/1120 SUCCESS via `verify-light.sh`.
- How far: all PLAN todos done. Ships the foundation for the Rigid behavior arc (B-1 → B-2 → B-3). No Simulator changes; no ExplicitSystem::update changes; no GeneralMesh changes — all three are B-3's surface.
  - **`include/Quat.hpp` created** — cut/paste from `src/main.cpp:1554`. Struct body byte-identical: `struct Quat { float w = 1.0f; float x = 0.0f; float y = 0.0f; float z = 0.0f; };`. Helpers (`operator*`, `quatNormalize`, `quatFromAxisAngle`, etc.) stay in main.cpp. The cut/paste is the minimum scope-expansion that lets `RigidPhysicsTypes.hpp` see a complete `Quat` for by-value storage of `RigidInitial::rotation` (forward-decl alone fails — aggregate members of class type need the type complete).
  - **`include/RigidPhysicsTypes.hpp` created** — POD types in `namespace ysim::physics`: `enum class RigidShapeType : int32_t { Box, Sphere, Plane, ConvexMesh, StaticMesh }`; `struct RigidShape` (type, half_extents, normal, mesh_vertex_data/count, mesh_index_data/count); `struct RigidInitial` (position, rotation, linear_velocity, angular_velocity, mass=1, friction=0.5, restitution=0, shape); `using BodyHandle = int32_t;` + `constexpr BodyHandle kInvalidBodyHandle = -1`.
  - **`include/NullRigidPhysicsBackend.hpp` created** — `class NullRigidPhysicsBackend` satisfying the 12-method contract per `docs/design/rigid_physics_backend.md` lines 16-52. Storage: `std::vector<RigidInitial> initials_` indexed by handle; `tinym::vec3 gravity_`; `bool initialized_`. Kinematic: `step()` no-op, `apply*` / `set*Velocity` no-ops, gravity stored but ignored. Per-body initial snapshot preserves the round-trip semantic. Out-of-range BodyHandle returns sentinel zeros / identity quat (CM-012 — no `exit()` in helpers; caller decides fatality).
  - **`src/main.cpp` wiring**: 3 new includes after `#include "tinym.hpp"` at line 478 (`Quat.hpp`, `RigidPhysicsTypes.hpp`, `NullRigidPhysicsBackend.hpp`). Local `struct Quat { ... };` definition at line 1554 deleted and replaced with a 5-line comment pointing to `include/Quat.hpp`. **Block 30 in `runSelfTest`** added immediately after Block 29 closes (~line 9081), **OUTSIDE the Metal-gated section** — pure C++ contract surface runs on macOS + Linux containers alike.
  - **Block 30 — 3 clauses**:
    1. **Clause 1 — Lifecycle**: `backendName()` returns `"Null"`; `initialize(gravity)` returns true; `setGravity(...)` doesn't crash; `shutdown() + re-initialize` doesn't crash. Pass label: `D-037 / NullRigidPhysicsBackend lifecycle: name + init + setGravity + shutdown + re-init`.
    2. **Clause 2 — Body state query**: `addBody(init)` returns valid handle (≥0); `getPosition / getRotation / getLinearVelocity / getAngularVelocity` round-trip the initial values within 1e-5 (position (0,5,0); rotation identity {1,0,0,0}; linear (1,2,3); angular (-0.5,0,0.5)). Pass label: `D-037 / NullRigidPhysicsBackend addBody+query: stored initial position/rotation/velocities round-trip`.
    3. **Clause 3 — step + force/impulse/setVelocity are no-ops**: after `applyForce(100,100,100) + applyImpulse(10,10,10) + setLinearVelocity(999,999,999) + setAngularVelocity(999,999,999) + step(1/60, 1)`, post-step queries match the addBody-time values within 1e-5. Stricter than design doc (rotation + velocity invariants, not just position) per PLANNER §7. Pass label: `D-037 / NullRigidPhysicsBackend step+force/impulse/setVelocity are no-ops (kinematic; B-2 Bullet + B-3 wiring enable dynamics)`.
  - **`docs/DECISIONS.md`**: D-037 appended (motivation linked to user observation on Rigid gravity gap; justification of "no Simulator changes in B-1"; mechanization summary; bug-probe shapes; new standing constraint; self-test baseline counting note explaining the 56 (actual) vs 57 (reported) baseline).
  - **`docs/TEST_MATRIX.md`**: BDD-008 row's test-address column updated with Block 30 + D-037 reference and explicit "Null backend is kinematic by design; row promotes to pass when B-3 lands" note. Status stays `pending`.
  - **`docs/roles/PLANNER.md`**: RIGID-BACKEND-PORTABILITY entry appended to Standing constraints subsection (last bullet, after BDD-102-vs-ALEMBIC-BYTES).
- What's tested:
  - **59/59 self-test PASS deterministic across 5 consecutive runs** from `build/` (56 prior + 3 D-037 clauses). The actual measured baseline at commit `186578d` is 56, not the "57" PROJECT_STATE.md / PLAN.md reported after the BDD-006 fix-turn — an off-by-one counting discrepancy in the prior slice's reported headline (the underlying clauses still PASS). B-1 lands on the corrected baseline.
  - Block 30 + Blocks 1-29 all green.
  - Doctest 159/159 (scene_io_test) + 1120/1120 (unit-test suite) SUCCESS via `verify-light.sh`.
- **Bug-probes (all LOAD-BEARING):**
  - **(a) addBody returns kInvalidBodyHandle**: Clause 2 FAILs with `handleOk=0` (and posOk/lvOk/avOk=0 because out-of-range handle returns zeros); Clause 3 cascades. Restored.
  - **(b) getPosition returns tinym::vec3{} unconditionally**: Clause 2 FAILs with `posOk=0`; Clause 3 FAILs with `posInvariantOk=0`. Restored.
  - **(c) step() mutates position by gravity.y*h*h**: Clause 3 FAILs with `posInvariantOk=0` ONLY (Clauses 1+2 still PASS because they don't pump step). Restored.
  - **(d) getRotation returns ::Quat{0,0,0,0} (zero, not identity)**: Clause 2 FAILs with `rotOk=0`; Clause 3 FAILs with `rotInvariantOk=0`. Restored.
- **Manual GUI test (user-driven, post-slice)**: Not required for B-1 — the slice is contract foundation only. Visible Rigid gravity effect lands at B-3 when the Bullet backend is wired through `Simulator::update`. Estimator turn 33 can proceed directly via `/codex:rescue`.
- Non-goals respected: NO Simulator template-parameter widening (no 4th `RigidBackend` parameter yet); NO ExplicitSystem::update Rigid-branch implementation (still no-op fall-through at src/main.cpp:5891); NO GeneralMesh::rigidBodyHandle field; NO Simulator::addRigidBody mutator; NO Bullet integration; NO actual physics in the null backend; NO BDD-008 promotion; NO new BDD/FR; NO new CM-NNN; NO Block-29 changes; NO C-* FlatBuffers caching work; NO retire of BDD-006-RIGID-DISPATCH-PARKED. All Blocks 1-29 byte-identical pass-list.
- What's next: Estimator turn 33 review via `Agent(codex:codex-rescue)`. Expected verdict: NOTE-clean (pure additive slice, no existing surface touched). Possible NOTE items:
  - (i) Block 30 placement (outside Metal gate) is a taste-level call; either is defensible.
  - (ii) Quat helpers (`operator*`, `quatNormalize`, etc.) stay in main.cpp — future cleanup with source-file split slice.
  - (iii) `RigidShape::mesh_vertex_data` / `mesh_index_data` as raw `const float*` / `const uint32_t*` — caller-owns-buffer dangling-pointer subtlety. D-037 mentions this; null backend never reads the pointers, but B-2's Bullet backend will snapshot.
  - (iv) Block 30 doesn't exercise ConvexMesh / StaticMesh shape types. Null backend ignores shape; B-2 will cover.
  - (v) Out-of-range handle behavior covered indirectly (Clause 2's handleOk=true assertion is the proxy); no explicit out-of-range clause.
  - (vi) Self-test baseline counting note in D-037 (prior "57" was off by one) — informational; the slice's count "+3" is correct.
