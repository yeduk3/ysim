# Project State

> Owner: **Planner**. Compact rolling summary so the next planning turn does not have to re-read every spec.
> Updated: 2026-05-13

## What ysim is (one paragraph)

A macOS-only simulation engine targeting cloth and rigid-body shots, sitting between Houdini (too complex) and Unreal (too real-time-biased). v1 ships an end-to-end author → simulate → save → reload → export-to-Alembic flow, with the scene format and behavior API kept structured enough to support a v2 LLM control surface. Dual-GPU architecture (OpenGL render + Metal compute), template-parameterized backends (`CPU`/`METAL`), no virtual dispatch on the hot path.

## Specs in place

- `docs/specs/PRD.md` — authored 2026-05-06. Seven sections (Problem, Outcome, Scope, Non-goals, Constraints, Success metrics, Open questions).
- `docs/specs/FRD.md` — 19 functional requirements, 1:1 with v1 capabilities + no-regression items.
- `docs/specs/BDD.md` — 19 paired user-stories + 3 cross-cutting (BDD-101 round-trip, BDD-102 determinism, BDD-103 backend-boundary).
- `docs/TESTS.md` — 22 Given/When/Then scenarios.
- `docs/TEST_MATRIX.md` — 22 rows, all `pending`.

## What's already implemented (per PRD §6 metric #3 — no-regression set)

- Metal-based collision detection: per-object and scene-wide LBVH (spatial hashing parked due to perf).
- Metal-based explicit Euler integration for cloth.
- Click-to-select objects via ray-cast; live color/behavior edits.
- `FrameProfiler` with named timing sections + GUI window + CSV export.
- Both `TriangularCloth` and `FastGridCloth` cloth variants exist; `Float` and `Rigid` behaviors are defined in the enum but Rigid is not yet wired through.

## Open questions blocking future plan slices

Carried forward from `PRD.md`. Each lists which slice it blocks.

- **Q1 — OpenPBR subset.** Resolved in practice by **D-005**: v1 ships base color, metallic, roughness, specular weight, emission color; persistence round-trips all five. Material UI slice (FR-005 / BDD-005) can proceed; PBR preview shader is the remaining open work.
- **Q2 — Cloth UX surface (one option vs two).** **Resolved 2026-05-13**: TWO distinct behavior tags in the dropdown — `TriangularCloth` (default) and `FastGridCloth` (only available for square regular grid primitives; grayed out / hidden for non-grid shapes). FastGridCloth is a performance tier, not a UX simplification. Unblocks BDD-006.
- **Q3 — Scene file format.** **Resolved (D-001): JSON**, vendored `nlohmann/json` under `include/nlohmann/`.
- **Q4 — Rigid backend default (Bullet vs Jolt).** **Resolved 2026-05-13: Bullet is the FIRST implementation behind a new `RigidPhysicsBackend` template-based contract.** Per user direction, the abstraction is template-based duck-typing (C++17), NOT virtual interfaces — compile-time monomorphic dispatch, zero overhead in the per-step hot path. Backend types satisfy a documented contract; consumer code (`Simulator`) is templated on the backend type. Runtime backend selection (CLI flag) uses `std::variant` + `std::visit` at the entry point only. Future Jolt / custom backends plug in by adding their type to the variant. Quat handling: each backend internally uses its native type (`btQuaternion` for Bullet, `JPH::Quat` for hypothetical Jolt), conversions happen at the backend boundary; the contract surface uses ysim's `::Quat`. **Full design in `docs/design/rigid_physics_backend.md`.** Slice stages: B-1 (contract + Null backend), B-2 (Bullet), B-3 (wire into Rigid behavior tag).
- **Q5 — Alembic schema specifics.** **Resolved 2026-05-13: target is Alembic Geometry Cache for Unreal import; FIRST implementation is FlatBuffers-based behind a new `MeshAnimationWriter` template-based contract.** Same compile-time-efficient pattern as the rigid backend (C++17 duck-typed contract, templated consumer, `std::variant` for runtime CLI selection only). FlatBuffers backend ships first so the writer interface + data-extraction-from-simulator path can be tested via round-trip (write FlatBuffers → read FlatBuffers → byte-compare) before the Alembic library integration lands. Alembic backend ships as Stage C-3; Unreal Geometry Cache compatibility lights up at that point. **Full design in `docs/design/mesh_animation_writer.md`.** Eventual Alembic schema: `OPolyMesh` constant-topology, FaceSets per material slot, Y-up storage, velocity-attribute deferred.
- **Q6 — Export FPS / time-step decoupling rule.** **Resolved 2026-05-13**: export FPS coupled to sim time-step. Default export = every simulation frame (FPS = 1 / sim.h, typically 60). Optional stride parameter for downsampling (e.g., sim at 60Hz, export every 2nd frame at 30fps). No upsampling — exported samples align with sim-integrator boundaries to preserve determinism.
- **Q7 — Save-file forward migration policy.** Does not block v1 (only v1→v2 transition).
- **Q-D (from `docs/ARCHITECTURE.md §5`) — Test backend ownership.** **Resolved (D-012): headless Metal harness via `ysim --self-test`**, with SKIP path on hosts without Metal so the Estimator's Linux container can still run `verify.sh`.

## Decisions (Planner-resolved, awaiting human confirmation)

- **Scene file format = JSON.** Reason: structured (each field named), human-diffable (PRD §5.2 forward-compat constraint), easy LLM target for v2, low integration cost. Alternative (binary) was rejected because it works against the LLM-addressability constraint. Hybrid (JSON + sidecar binary) was rejected as premature optimization for v1 — scene files don't carry heavy data (mesh imports are referenced by path).
- **New primitives default to `Float` behavior.** Reason: prevents surprise on creation (a newly-spawned cube doesn't immediately fall through the floor before the user can assign anything to it).
- **Imported meshes are referenced by file path, not embedded.** Reason: smaller scene files, predictable diff. Cost: moving an asset breaks load. Acceptable for v1; revisit if user reports breakage.

## Next milestone

**In flight: D-042 R-2 Wire MeshGL to PreviewState (`feat/d-042-r-2-meshgl-preview-binding`)** (planned 2026-05-14). Second slice in the 7-step R-* sequence that refactors Scene/Simulator into a 2-level structure (Scene = static editable state + previews; Simulator = lazy-initialized engine driven by Scene::pack). R-1 (08f3ef5) shipped `PreviewState<PR>` as a heap-owned per-mesh vertex/facet/normal buffer populated by initializer at addGeneralMesh time. R-2 adds `MeshRenderState::registerPreviewBinding(id, xPtr, numVerts, facetPtr, numFacets, normalPtr)` CPU-only API + `previewBinding(id)` accessor + `removeById(id)`. Each `Simulator::addX` wrapper (addCube/addSphere/addCloth/addClothFile/addClothGridFast/addFloatMesh/addGround) + `loadScene` calls `registerPreviewBinding` with `request.preview.*Ptr()` right after `scene.addGeneralMesh`. `getOrCreate(mesh)` prefers preview binding over packed sub-views at MeshGL construction time. `Simulator::initialize`'s `renderState.clear()` band-aid RETIRES (preview pointers are heap-stable across Scene::pack reallocations). `Simulator::removeMesh` calls `removeById` for memory hygiene. Block 37 verifies binding is registered pre-initialize (pure CPU; no GL context needed). Self-test count 66 → 67. Future R-* slices outlined in D-042 R-2's DECISIONS entry: R-3 rewrites Scene::pack to memcpy preview → packed; R-4 routes translate/rotate/setMaterial through preview; R-5 adds packed→preview resync at update end; R-6 migrates self-test reads; R-7 cleanup. Worktree: `.claude/worktrees/r2-meshgl-preview` (branched off `08f3ef5`). Commit prefix `add:`. D-042 R-2.

**Shipped slice (2026-05-14): D-042 R-1 PreviewState infrastructure** — merged to `main` via commit `08f3ef5`. New header `include/PreviewState.hpp` (~85 lines): heap-owned per-mesh `std::vector<PR>` x/n + `std::vector<uint32_t>` facets + `recomputeNormals()` helper. `GeneralMeshInitializer::populatePreview` virtual default no-op; 4 subtype overrides (Cube/Sphere/Grid/File). `Scene::RequestGeneralMesh` gains `preview` field; `addGeneralMesh` invokes `initializer->populatePreview(request.preview)` immediately after emplace. Block 36 verifies preview populated for all 4 initializer kinds. Self-test count 65 → 66 PASS deterministic on macOS AND Linux. Estimator turn 36 returned NOTE: 2 NOTEs flagged populate-geometry duplication + .obj re-read (both deferred to R-7 cleanup).

**Previously in flight: B-3 Wire Rigid behavior through Simulator (`feat/b-3-wire-rigid-behavior`)** (shipped 2026-05-14). **This is the slice that closes the user's "Rigid bodies should fall under gravity" goal.** `GeneralMesh<BE, PR>` gains `rigidBodyHandle = -1` + `rigidLastBodyPos`. `Simulator<BE, PR, System>` gains a hard-coded `ysim::physics::EulerRigidPhysicsBackend rigid_` member (no template-param widening this slice; future Bullet B-2.1 swap is one-line). New idempotent helper `Simulator::ensureRigidBackendBody(meshId)` builds `RigidInitial{position=transformPosition, rotation=rotationQuat, mass=1, shape=Sphere(radius=inferRigidRadius)}` + `rigid_.addBody`. Triggers: (i) `changeBehavior(meshId, Rigid)` calls the helper; (ii) `Simulator::initialize` resets backend with `scene.environment.gravity` and sweeps all Rigid meshes (covers persistence + post-pack-rebuild). `changeBehavior(meshId, Float)` clears the rigid linkage on the mesh side (backend slot leaks per D-038, acceptable). `Simulator::update` calls `rigid_.step(h, 1)` once per outer frame, then for each Rigid mesh: `Δpos = backend.getPosition - rigidLastBodyPos` applied to `state.x` + `state.xPrev` + `transformPosition`. `applyEnvironmentForces` skips Rigid (zeroes externalForces, mirroring Float pattern). **Translation only** — rotation-correct vertex update is a future slice. Block 32 mechanizes the end-to-end test (`addCube(0,5,0) + changeBehavior(0, Rigid) + 30 sim.update() → witness vertex.y dropped > 0.5m`). Self-test count 61 → 62. **Retires BDD-006-RIGID-DISPATCH-PARKED + BDD-018-BEHAVIOR-TAG-PARKED** (both standing constraints close). BDD-008 row promotes `pending → pass`. Worktree: `.claude/worktrees/b-3-rigid-wire` (branched off `481e013`). Commit prefix `add:`. D-039.

**Shipped slice (2026-05-14): B-2′ EulerRigidPhysicsBackend (B-2 pivot from Bullet)** — merged to `main` via commits `5c745fa add:` + `481e013 chore:`. Estimator turn 34 returned WARNING (BDD-008 scope-coverage gap; matrix correctly stays pending until B-3) + 1 NOTE (removeBody slot-leak; applyForce==applyImpulse). Pivoted from the planned Bullet vendor because the Bash classifier denied `git submodule add` even after AskUserQuestion authorization. New header `include/EulerRigidPhysicsBackend.hpp` (~185 lines): semi-implicit Euler integrator implementing the D-037 12-method contract; Sphere-only y=0 ground clamp; mass=0 static; inlined Quat update with normalize fallback; CM-012 discipline. Block 30 (Null backend, 3 clauses) RELOCATED above the Metal-less SKIP gate at src/main.cpp:6164; new Block 31 (Euler backend, 2 clauses) sits immediately after. Both blocks now run on Linux verify.sh (folds Estimator turn 33 WARNING). Self-test count 59 → 61 PASS deterministic on macOS AND Linux. 4 bug-probes verified load-bearing. PARALLEL-IMPL-LOCKSTEP extended (Euler's inline Quat math mirrors main.cpp's D-019 helpers). RIGID-BACKEND-PORTABILITY now load-bearing across Null + Euler. Bullet remains canonical per design doc §B-2 — deferred to a future B-2.1 slice once vendor permission rule is added. D-038.

**Shipped slice (2026-05-14): B-1 RigidPhysicsBackend contract + Null impl (`feat/b-1-rigid-physics-backend-contract`)** — merged to `main` via commits `8a44275 add:` + `43ccb7f chore:`. Estimator turn 33 returned WARNING with 0 BLOCK + 1 WARNING + 3 NOTE. WARNING: `runSelfTest`'s Metal-less SKIP gate at src/main.cpp:6164 short-circuits before Block 30, so the null contract is not exercised on Linux container despite Block 30 sitting "outside the inner Metal-using blocks". Folded into B-2 via Block 30 relocation. NOTE items informational (clean Quat extraction; raw mesh-buffer pointer subtlety for B-2; stricter-than-design rotation+velocity invariants). Three new headers under `include/`: `Quat.hpp` (cut/paste from src/main.cpp:1554; struct body byte-identical), `RigidPhysicsTypes.hpp` (POD types in `namespace ysim::physics` — RigidShapeType enum + RigidShape + RigidInitial + BodyHandle alias + kInvalidBodyHandle = -1), `NullRigidPhysicsBackend.hpp` (12-method contract no-op impl; per-body initial snapshot via `std::vector<RigidInitial>`; kinematic step; CM-012 discipline). Block 30 (3 clauses: lifecycle / body-state-query / step-is-noop). 56 (actual baseline; prior "57" was off-by-one) → 59 self-test PASS. 4 bug-probes verified load-bearing + restored. New standing constraint **RIGID-BACKEND-PORTABILITY**. D-037.

**Shipped slice (2026-05-14): BDD-006 BLOCK fix-turn on `feat/bdd-006-behavior-assignment-ui`** — merged to `main` via commits `2e1b31b fix:` + `186578d chore:`. Estimator turn 32 returned NOTE with 2 informational items (variant-alternative future-hardening for B-3; meshBehaviors pre-state assertion in Clause 6 deliberately omitted). Original D-036 slice + the turn-32 fix-turn rolled into a single `fix:` commit per D-035 turn-30 precedent. BLOCK closed via "option (b) load-side accept Rigid": `scene_format::isReservedBehavior` narrowed `{Rigid, Elastic, Fluid, Generator}` → `{Elastic, Fluid, Generator}`; `isKnownBehavior` widened to include `"Rigid"`; new `loadScene` Rigid branch sets `btype = Rigid + bparams = FloatBehaviorParams<PR>{}`. WARNING closed via `syncBroadPhaseCaches` lambda in `Simulator::changeBehavior` that writes `objTrees[idx].objBehavior` AND `shBroadPhase.meshBehaviors[idx]` directly. Block 29 grew 4 → 6 clauses (Rigid round-trip + cache-sync); 55 → 57 self-test. `test/scene_io_test.cpp` BDD-016 reserved-rejection example swapped Rigid → Elastic to preserve test intent. D-036 turn-32 fix-turn addendum; no new D-NNN/CM-NNN. BDD-006-RIGID-DISPATCH-PARKED standing constraint narrowed (persistence done; only integrator dispatch parked until B-3).

**Shipped slice (2026-05-13): Role-doc maintenance pass (`chore/role-doc-maintenance-pass`)** — merged to `main` via commits `ed0d6f0` chore + `b628154` estimator turn 31. Pure docs slice; folded 5 discipline learnings from slices 17–30 (PROBE-COVERAGE-EDGES, PARALLEL-IMPL-LOCKSTEP canonicalized into PLANNER's new "Standing constraints" subsection, BLOCK-FIX-TURN cadence, UTILITY-HELPER-EXIT-IS-CALLERS-DECISION, Standing-fold-in candidates list canonicalized). Estimator turn 31 returned cleanest verdict: NOTE with no items.

**Chore commit (2026-05-13): Design docs for `MeshAnimationWriter` + `RigidPhysicsBackend` template-based contracts** — direct commit `037d83f` on `main` (no slice cycle; matches `9b76f7b` / `4217619` precedent). Captures Q4 + Q5 resolutions: Bullet is FIRST rigid impl behind template-based contract; FlatBuffers is FIRST mesh-cache impl behind template-based contract; Alembic backend ships at Stage C-3. Full designs in `docs/design/mesh_animation_writer.md` + `docs/design/rigid_physics_backend.md`. Quat marshalling at backend boundary (per user direction); `std::variant` + `std::visit` for runtime backend selection. No code changes — design only.

**Shipped previous slice (2026-05-13): Inspector rotation ergonomics slice (D-035) — BLOCK-fix-turn on top of original D-035 (`feat/inspector-rotation-ergonomics`)** — merged to `main` via commits `1f21335` fix + `282780e` estimator turn 30. Mechanizes FR-004 Notes via 3-mode rotation panel (Quat / Euler XYZ / Axis-Angle). New free-function helpers next to D-019's quaternion family: `quatFromAxisAngle`, `quatToAxisAngle`, `quatFromEulerXYZ`, `quatToEulerXYZ` (Quat-based, src/main.cpp:~1605) + parallel float-array versions (`axisAngleDegToQuatWxyz` etc.) in `include/MeshInspectorWindow.hpp` so the inspector TU doesn't need access to main.cpp's bare `Quat` struct. **D-035** records conversion conventions: Euler XYZ intrinsic Tait-Bryan (Blender-compatible); axis-angle output [0, 180°] with auto-normalized axis; identity-quat fallback returns angle=0 axis=(1,0,0); gimbal-lock fallback lossy by design. **Turn-30 BLOCK fix** (same commit, antipodal canonicalization at extractor entry): when q.w < 0, negate all 4 components — eliminates the divide-by-zero NaN at q = (-1, 0, 0, 0) (360° rotation case). Applied lockstep to both parallel implementations; Block 28's AxisAngle clause extended with antipodal-equivalent comparison + 2 direct-constructed negative-w probes. Bug-probe verified: reverting the negate-on-w<0 block reproduces the turn-30 BLOCK signature `axisOut=(NaN,NaN,NaN) finite=0`. Folded turn-29 NOTE-1: `Program::cleanUp()` now zeros `tessControlShaderID` + `tessEvalShaderID`. 49 → 51 self-test count. Estimator turn 30 (re-issued post-fix-turn) returned NOTE: parallel-implementations lockstep constraint persists as documented limitation until source-file split. BDD-018 + BDD-004 matrix rows still `pass`. Estimator turn 30 BLOCKed the slice: axis-angle inverse helpers (`quatToAxisAngle` in `src/main.cpp:~1635` and `quatWxyzToAxisAngleDeg` in `include/MeshInspectorWindow.hpp:~61`) don't canonicalize the `q.w < 0` antipodal case — a 360° rotation produces `q = (-1, 0, 0, 0)`, then `s = sqrt(1 - qw*qw) = 0` and `axis = wxyz[xyz] / s = NaN/±inf` on the next display pass. Fix-turn (this PLAN.md) negates all four components at function entry when `q.w < 0`; after canonicalization, `qw ∈ [0, 1]` and `acos(qw) ∈ [0, π/2]` doubled = `[0, π]` (D-035's documented convention is now enforced by construction). Applied to BOTH parallel implementations (Quat-based + float-array) in the same commit per the parallel-implementation lockstep constraint from turn-30 NOTE. Block 28's AxisAngle clause is extended with antipodal-equivalent comparison + 2 negative-w probes (antipodal identity and antipodal 90°-Y); self-test count stays 51 → 51 (probes added to existing clause, no new pass labels). D-035 entry in DECISIONS.md gets a turn-30 addendum paragraph. No new D-NNN, no new BDD/FR. Commit prefix `fix:` (BLOCK fix-turn stays on the same branch). Below is the original slice's plan summary for context — most of it ships unchanged on top of the fix-turn. Plan in `.agent/PLAN.md`. Mechanizes FR-004 Notes — adds Euler XYZ + Axis-Angle input affordances to the inspector's rotation panel. Design call: Shape B (inspector converts in-widget to Quat then fires the existing `on_rotate` callback; `MeshInspectorTarget` surface untouched). New free-function helpers next to D-019's quaternion family: `quatFromAxisAngle`, `quatToAxisAngle`, `quatFromEulerXYZ`, `quatToEulerXYZ`. Conventions documented in new D-035: Euler XYZ is intrinsic Tait-Bryan (R_z * R_y * R_x, matches Blender); axis-angle output is in [0, π] with auto-normalized axis; identity-quat fallback returns angle=0 axis=(1,0,0); gimbal-lock fallback for Euler extraction is lossy by design. `MeshInspectorWindowState` gains `int rotation_input_mode = 0;` field (0=Quat, 1=Euler, 2=AxisAngle). `mesh_inspector_gui.cpp` rotation panel gains a 3-mode toggle; each mode reads `*target.rotation_wxyz`, renders the appropriate widget, on commit converts back to Quat and fires `target.on_rotate`. NEW Block 28 in `runSelfTest` asserts Quat ⇌ AxisAngle and Quat ⇌ Euler XYZ round-trip within `1e-5` for non-singular inputs; 49 → 51 self-test count. Folds turn-29 NOTE-1: tighten `Program::cleanUp()` to also zero `tessControlShaderID` and `tessEvalShaderID` (~4 lines). Turn-29 NOTE-2 (`glfwInit` non-ref-counting) and turn-28 NOTE (duplicated `buildSelectedMeshTarget` shape) deliberately NOT folded. Widget rendering itself is manual-test-only (no headless ImGui); the harness gate covers only the math layer. Bug-probes: (a) swap q.x/q.y in `quatFromAxisAngle` → round-trip clause FAILs; (b) change Euler `from` order without changing `to` → round-trip FAILs. New D-035 (conversion conventions). No new BDD/FR. No matrix row changes.

**Shipped slice (2026-05-13): Loadshader skip-safety + HiddenGLContext cleanup slice (`fix/loadshader-skip-safety`)** — merged to `main` via commits `9b18fee` fix + `24c47ac` estimator turn 29. Closed Estimator turn-27 WARNING + NOTE. Recorded **D-034** (`Program::printLog` no longer calls `exit(1)`; `linkShader` calls `cleanUp()` after `printLog` on link failure so `programID` becomes 0; each `loadShader` overload early-returns after each `loadShaderOf`; `HiddenGLContext` gains `bool glfwInitialized` for symmetric `glfwTerminate` on destruction). Production main shader load (`src/main.cpp:8460`) got defensive `if (!programID) std::exit(1)` to preserve loud-failure behavior; the 4 `debugLineShader` call sites are pre-guarded so retry-next-frame is acceptable. New Block 27 mechanizes D-034 by invoking `loadShader` with bogus filenames + asserting `programID == 0`. 48 → 49 self-test PASS deterministic. Bug-probe (a) loader load-bearing — re-introducing `exit(1)` makes the harness abort inside Block 27 (only 48 PASS emit instead of 49). New CM-012 records the "utility helper calls exit(1) defeats SKIP semantics" trap. Estimator turn 29 returned **NOTE** (first clean verdict in many turns) with two NOTE items: (1) `Program::cleanUp()` omits `tessControlShaderID + tessEvalShaderID` (footgun if tess overloads get used); (2) `glfwInit` non-ref-counting limitation already documented in header. NOTE-1 is folded into the in-flight inspector ergonomics slice; NOTE-2 stays documented-not-enforced.

**Shipped slice (2026-05-13): BDD-018 inspector live-edit propagation slice (`feat/bdd-018-inspector-live-edit`)** — merged to `main` via commits `dcb7382` add + `1d8b6d9` estimator turn 28. NEW Block 26 in `runSelfTest` constructs `mesh_inspector::MeshInspectorTarget` matching the production shape at `src/main.cpp:8349-8387` exactly, invokes each callback (`on_material_edit` / `on_translate` / `on_rotate`) directly with synthetic edit values, and asserts (a) exactly one Simulator setter call per callback fire (stricter than spec literal per PLANNER.md step 7), (b) live in-place pointer mutation, (c) `sim.update()` survival without `sim.initialize()`. Three pass clauses (material / translate / rotate); 45 → 48 self-test count. Bug-probes (a)/(b)/(c) verified — stubbing each Simulator setter inside its lambda body loud-FAILs the corresponding clause. Spec-substitutions documented in Block 26 comment + matrix row + PLAN.md: behavior-tag clause parked under BDD-006/Q2 (in-place behavior switching not yet wired into `MeshInspectorTarget`); "next rendered frame" mechanized as in-place pointer value the renderer reads each frame (no FBO render needed — load-bearing claim is on input-data path); "must not require pause/resume" mechanized as no `sim.initialize()` between callback fire and pump. Pure harness slice — no production-side changes, no new D-NNN, no new CM-NNN. BDD-018 matrix row promoted `pending → pass`. Estimator turn 28 returned WARNING (1 WARNING + 1 NOTE): WARNING is the standing parked behavior-tag gap (BDD-006/Q2-blocked, NOT new regression); NOTE on duplicated `MeshInspectorTarget` wiring between production and Block 26 (future cleanup when source-file split lands).

**Shipped slice (2026-05-12): FBO-based render harness slice (`feat/fbo-render-harness`)** — merged to `main` via commits `5c7c46a` add + `01092b9` estimator turn 27. Recorded **D-032** (new `include/HiddenGLContext.hpp` parallel to `YGLWindow`; new Block 25 in `runSelfTest` builds hand-rolled cube + 256×256 RGBA8 FBO via existing `Framebuffer`; renders D-028 PBR path twice at roughness ∈ {0.1, 0.9} with metallic=1 silver-ish material; asserts max per-channel byte diff > 30; SKIP-safe on GL-init / shader-load failure; bug-probe (b) comment-out the roughness uniform setter loud-FAILs — proves D-028's shader consumes the uniform per-render). Recorded **D-033** (production `bottomUpHybridDepth` raised 3 → 30 based on D-031's measurement chart). 45/45 self-test PASS deterministic across 5 runs. Folded turn-26 WARNING fixes: D-031 "Measured result" paragraph corrected (HybridD2 ≈ 673 ms IS faster than FullCPU ≈ 731 ms at 500k by ~8%, but both dominated by FullGPU ≈ 353 ms by ~2×); README.ko.md placeholder line rewritten. BDD-005 matrix row promoted `warning → pass` — closes the standing structural WARNING D-028 introduced. Estimator turn 27 returned WARNING (1 WARNING + 1 NOTE): WARNING on `Program::loadShader`'s `exit(1)`-on-failure (Block 25's documented `programID == 0` SKIP path may not actually skip); NOTE on `HiddenGLContext` destructor not calling `glfwTerminate()` on early-init failure. Both queued for a future small `fix/loadshader-skip-safety` slice — explicitly NOT folded into the BDD-018 in-flight slice per user direction.

**Shipped slice (2026-05-12): BVH refit benchmarking harness slice (`feat/bvh-refit-bench`)** — merged to `main` via commits `6ca056d` add + `bb3b667` estimator turn 26. Recorded **D-031** (CLI flag `--bench-bvh-refit`; new `BVHRefitMethod` enum + `BenchConfig` struct + `runRefitBench` function; reads `broad_refit` from FrameProfiler; output under `profiles/experiment/bvh-refit-2026-05-12/` with Korean README + self-contained matplotlib chart.py + populated CSV + 2 PNG charts). Block 24 is a 1×1×1 smoke test (mechanism-only). 44/44 self-test PASS deterministic across 5 runs. Full sweep completed in ~109 seconds; **measured finding (Apple Silicon Metal 3.2):** at 1k–10k all four methods are noise-equivalent in a ~25–60 ms band; at 100k FullGPU wins ~1.8× over CPU/Hybrid; at 500k FullGPU wins ~2.0× (FullCPU ≈ 731 ms, HybridD1 ≈ 788 ms, HybridD2 ≈ 673 ms — HybridD2 marginally beats FullCPU but loses to FullGPU ≈ 353 ms by ~2×). D-030's hybrid-sweet-spot hypothesis is contradicted; FullGPU dominates at any size where the measurement is non-noise. Estimator turn 26 WARNING flagged an interpretation error in D-031's "Hybrid D=1/D=2 never beat FullCPU" prose (HybridD2 ≈ 673 ms IS faster than FullCPU at 500k) and a stale "placeholder" line in README.ko.md (CSV has 160 rows committed) — both folded into the in-flight FBO slice.

**Shipped earlier slice (2026-05-12): hybrid BVH bottom-up combine slice (`feat/bvh-bottomup-hybrid`)** — merged to `main` via commits `47ce9b4` add + `febc19b` estimator turn 25. Recorded **D-030** (parallel-symbol restructure: new `bottomUpBoxesPartial` kernel + `bottomUpBoxesPartialGPU` C++ method live alongside the unchanged D-029 `bottomUpBoxes` + `bottomUpBoxesGPU`; new `bottomUpHybrid(sceneBox, maxDepth)` driver dispatches between pure-CPU `bottomUpCombine()` for maxDepth=0, hybrid GPU+CPU for 1≤maxDepth<log2(N), and walk-to-root for maxDepth≥log2(N); CPU completion via `bottomUpCombineWithSkip()` short-circuits where `treeVisitCounts == 2`; per-BVH runtime knob `int bottomUpHybridDepth = 3`; `MetalGlobalContext::commitAndWait` gained null-encoder guard for the N≤1 / maxDepth=0 paths). Block 23 sweeps D ∈ {0, 1, 2, 1000} for correctness across all four modes. 43/43 self-test PASS deterministic across 5 runs. Mid-slice process correction: an earlier draft modified `bottomUpBoxes` + `bottomUpBoxesGPU` in place with a `maxDepth` parameter — user flagged this as a violation of the "creation verbs = ADD NEW parallel symbol" rule, the partial-depth path was restructured into its own kernel + method, and the rule was baked into `.claude/skills/slice/SKILL.md` ("Interpreting the user's slice request") + the `feedback_make_means_add_new` auto-memory entry. Estimator turn 25 WARNING (sole item): default `bottomUpHybridDepth=3` is unmeasured — closed by the in-flight bench slice.

BVH bottom-up GPU combine slice (`feat/bvh-bottomup-gpu`) merged to `main` on 2026-05-12 (commits `3feb747` fix + `0ff03a4` estimator turn 24); recorded D-029 (atomic-fetch-add single-dispatch + Metal 3.2 seq_cst fences for build AND refit + N=1 guard from the fix-turn). ~140 lines multi-pass scaffolding retired. Block 21 (per-interior-node bit-equality vs CPU reference on tess=2 cube) + Block 22 (N=1 single-triangle import) + Block 13's simplified BDD-010 assertion (cumNarrow > 0 with `enableSelfCollisions = false` Simulator default → inter-object pair by definition). CM-011 reframed as forensic: OLD CPU refit's 2-substep BVH lag was a deferred-commit artifact, not a contract; NEW GPU refit ships the standard 1-substep lag. 42/42 self-test PASS deterministic. Estimator turn 24 WARNING: Block 22's negative bug-probe is Apple-Silicon-masked (Metal shared storage absorbs OOB atomic reads + silently drops OOB writes); production guard is spec-correct regardless.

PBR preview shader slice (`feat/pbr-preview-shader`) merged to `main` on 2026-05-11 (commits `098c37d` add + `eb1413e` estimator turn 22); recorded D-028 (GGX-Smith microfacet + single directional light + individual uniforms + manual-test mechanization). `src/shader/shader.frag` rewritten as `pbrPreview()` consuming all 5 D-005 material uniforms; `MeshGL::draw` signature extended to 5 primitives; `SceneEnvironment` gains `lightColor` + `lightIntensity` fields (in-memory only, not yet persisted in scene_format) with Environment-panel ColorEdit3 + SliderFloat widgets. User's manual visual gate confirmed roughness change produces visible specular-highlight broadening. Initial tuning was too dark — `Li = lightColor / 256.0` + `ambient = 0.03 * baseColor` corrected to direct-radiance upload (`Li = lightColor` with C++ multiplying `lightColor * lightIntensity` before upload) + `ambient = 0.10 * baseColor`. Matrix row BDD-005 stays `warning` (renderer-side clause is manual-test-only — **standing structural WARNING** introduced, parallel to BDD-102's Alembic-bytes substitution). 40/40 self-test PASS deterministic. Estimator turn 22 WARNING was environmental (Codex sandbox's `$HOME` lacks write access for clang module cache); the PBR work itself passed inspection. NOTE about a stray bvh.metal whitespace change was WIP for the next slice, stashed during close-out and restored on the BVH slice branch.

FR-005 material-edit data-layer subset slice (`feat/material-inspector-data-layer`) merged to `main` on 2026-05-11 (commits `a252cf3` add + `2c33599` estimator turn 21); recorded D-027 (`Simulator::setMaterial(meshId, Material)` mutator mirroring D-014/D-021 — writes `mesh->material` AND `pendingMaterials[meshId]`; `MeshInspectorTarget` gains 4 new pointer fields + `on_material_edit` callback; `src/mesh_inspector_gui.cpp` renders 2 ColorEdit3 + 3 SliderFloat widgets with per-change firing). Block 20 mechanizes 4 pass clauses (setMaterial writes / sim step preserves / save+load round-trips / Scene::pack rebuild preserves). 40/40 self-test PASS deterministic across 5 runs; bug-probe verified both the pendingMaterials write-back AND the matEqual lambda. Matrix row BDD-005 promoted `pending → warning` (renderer-side clause parked, addressed by this slice). Estimator turn 21 WARNING flagged the skill infrastructure changes as out-of-scope — split out into separate chore commit `9b76f7b`.

Skill workflow infrastructure chore (commit `9b76f7b`) merged to `main` on 2026-05-11. Renamed `.claude/skills/{Planner,Generator,Estimator}` → lowercase variants. Added `.claude/skills/slice/SKILL.md` — composite skill that orchestrates the full Planner → Generator → Estimator cycle in one invocation (8 steps; halts on State C "Estimator hasn't run" or State E "mixed working tree"; Estimator independence preserved by always invoking via `Agent(codex:codex-rescue)`, never via local `Skill(estimator)`). Reduces manual-step burden of the 3-role workflow.

CM-008 production-side fix slice (`fix/cm-008-broadphase-skip`) merged to `main` on 2026-05-10 (commits `668dc4f` fix + `de83164` estimator turn 20); recorded D-026 (Shape A — `Scene::lifetimeMeshCount` never-resetting monotone counter; `mesh.lifetimeId` separate from `mesh.id` to preserve D-018 RNG seed semantic; `TRI_LBVH::builtForLifetimeId` cached at build; `BroadPhase::build`'s Float-mesh skip gains the lifetimeId match conjunct). The harness's 7 `objTrees.clear()` workaround sites removed (Blocks 14a/14b/15/16a/16b/17/18); CM-008 graduated to OLD_MISTAKES.md under new high-level cause "skip optimizations silently inherit prior-iteration identity when slot indices align." Block 19 mechanizes the scene-swap-at-same-count round-trip without manual clear. Bug-probe verified: reverting the lifetimeId clause produces 5 simultaneous FAILs (Block 19 + four other blocks whose workarounds were removed). 36/36 self-test PASS deterministic across 5 runs. Estimator turn 20 NOTE: BDD-017 row prose stale-bit → folded into next slice.

Rotate pack-roundtrip slice (`feat/rotate-pack-roundtrip`) merged to `main` on 2026-05-10 (commits `1650753` fix + `7d45c6b` estimator turn 19); recorded D-025 (Shape B — `Simulator::initialize()` auto-calls `applyPendingMaterials()`; `applyPendingMaterials()` now re-applies rotation via `rotateObject` not just by setting `rotationQuat`; `rotateObject` writes back to `pendingRotations[id]` after its state.x mutation). Closed the 5-slice deferral chain (PLANNER.md procedure step 6 escape-pattern detection forced action). Block 18 mechanizes the round-trip; bug-probe verified. 35/35 self-test PASS deterministic. Estimator turn 19 NOTE-clean (only the standing Metal-less-host SKIP note).

Triangle-precision click-pick slice (`fix/click-triangle-precision`) merged to `main` on 2026-05-10 (commits `bac733c` fix + `d54cb44` estimator turn 18); recorded D-024 (`BVH::queryClickRay`'s leaf does ray-vs-triangle Möller–Trumbore via existing `positions.ptr`/`primitives.ptr` slices into mesh storage; the leaf write `clickRayCollisions[]` carries the triangle-tmin, not AABB-tmin) + CM-010 ("BVH leaf must do primitive-vs-ray, not just AABB-tmin write" trap pattern for future BVH variants). User's intuition ("just query ray to BVH") was correct — no consumer-side helper, no two-stage walk; the BVH produces triangle hits directly. Block 17 mechanizes the user's reported scenario (size-30 ground tilted 60°-X around y=-1 pivot — Plane AABB-tmin = 2.5 vs cube 9.75; triangle-tmin = 10.577 loses to cube 9.75). Bug-probe verified. 34/34 self-test PASS deterministic. Estimator turn 18 WARNING was the BDD-017 row test-address cross-reference (folded into next slice).

Refit-after-edit fix-turn (`fix/refit-after-edit`) merged to `main` on 2026-05-10 (commits `ab0cae4`); closed Estimator turn-17 WARNING (translateObject/rotateObject didn't refit BVH, click-pick on paused sim hit old pose). Block 16 mechanized the invariant for both functions. New D-023. (Note: this fix-turn unblocked discovering the deeper triangle-precision regression that the click-triangle-precision slice then closed.)

FR-004 UI rotate-object slice (`feat/rotate-object`) merged to `main` on 2026-05-10 (commits `d550f82` add + `9c8da75` estimator turn 17); recorded D-021 (`Simulator::rotateObject` API) + D-022 (`quatConjugate` + `rotateVector` helpers). 31/31 self-test PASS deterministic. Estimator turn 17 WARNING surfaced the click-pick refit gap — closed by refit-after-edit fix-turn. The user's manual GUI test then surfaced the deeper triangle-precision regression that `fix/click-triangle-precision` addressed.

BDD-017 ray-pick slice (`feat/bdd-017-ray-pick`) merged to `main` on 2026-05-10 (commits `eb496d0` add + `3e5f6a4` estimator turn 15). BDD-017 coverage fix-turn merged to `main` on 2026-05-10 (commits `d593f2b` fix + `9f368b2` estimator turn 16); closed turn-15 WARNINGs and shipped D-020 (BVH leaf-return) as an unplanned production-fix scope expansion. 29/29 self-test PASS deterministic. Role docs (`docs/roles/{PLANNER,GENERATOR,ESTIMATOR}.md`) updated with discipline learnings from the last 7 slices in commit `4217619`.

BDD-004 quaternion slice merged 2026-05-09 (commits `ea4d1e4` + `089279b`); recorded D-019 (canonical Hamilton-product + normalize/norm). BDD-102 determinism slice merged 2026-05-09; closed CM-007, recorded D-018. Standing WARNING (state.x vs Alembic-bytes substitution) acknowledged as not foldable until FR-013 ships.

Profiler pause-gate slice (`fix/profiler-pause-gate`) merged to `main` on 2026-05-09 (commits `ed70e95` fix + `288b4a1` estimator turn 10); closed Estimator turn-8 WARNING, recorded D-017 (`ProfilerFrameGate` RAII guard shared between production and harness). Estimator NOTE-clean. 23/23 self-test PASS.

Cloth-thickness-band slice (`fix/cloth-thickness-band`) merged on 2026-05-09 (commits `910075b` + `ea5e973`); closed CM-006, recorded D-016 (vn-zero and position-push share the `(distance < thickness)` gate). Translate-pack + BDD-019 slice merged on 2026-05-09 (commits `cf6101d` + `b0de64c`); closed turn-7 WARNINGs, recorded D-015. Translate-object slice merged on 2026-05-08; closed BDD-003, recorded D-014. Cloth-CCD slice merged on 2026-05-08; closed BDD-007 + CM-005, recorded D-013. The standing-BLOCK era is over.

Standing feature candidates after this slice, ordered by what closes the most v1 acceptance:
- **B-2 Bullet RigidPhysicsBackend impl** — CURRENT slice (in flight). See "Next milestone" section above for full scope. Vendors Bullet 3.25 at `third_party/bullet3/` (D-038), adds Block 31 (2 dynamics clauses), and folds Estimator turn 33 WARNING by relocating Block 30 above the Metal-less SKIP gate.
- **B-3 Wire Rigid behavior tag into Bullet backend** — next slice after B-2 ships. Closes the gap that motivates the entire B-arc. Adds `int32_t rigidBodyHandle = -1` to `GeneralMesh<BE, PR>`; new `Simulator::addRigidBody(...)` mutator mirrors `addCloth` / `addCube`; widens `Simulator` template to `template <typename BE, typename PR, typename SystemT, typename RigidBackend = NullRigidPhysicsBackend>` (or BulletRigidPhysicsBackend depending on default-backend choice at B-3 time); `ExplicitSystem::update`'s Rigid branch reads `rigid_.getPosition / getRotation` and updates `state.x` via D-021's rotateVector path. Persistence (toSnapshot / loadScene) snapshots `rigidBodyHandle` indirectly through the initial position + rotation (handle re-acquired on load via `addBody`). **Retires BDD-006-RIGID-DISPATCH-PARKED + BDD-018-BEHAVIOR-TAG-PARKED.** BDD-008 row promotes `pending → pass`. Block 13 (BDD-010) and Block 6 (BDD-007) start exercising rigid-tagged scenes. See `docs/design/rigid_physics_backend.md` §B-3. **This is the slice that satisfies the user's "Rigid bodies should fall under gravity" goal.**
- **Source-file split slice** — move main.cpp's classes (Simulator, BVH, BroadPhase, Scene, etc.) into their own headers/TUs, then split `runSelfTest` + `runRefitBench` into per-class test files. Currently all tests inline in `runSelfTest` per the "tests-in-main.cpp" convention; main.cpp has grown beyond ~7900 lines and the convention is straining. User flagged this during the bvh-refit-bench slice brief ("이런 테스트들은 나중에 main 안에 있는 클래스들이 파일로 쪼개지면 테스트도 모두 분리해낼 계획임. 이것은 추후의 계획으로 우선 적어놓자."). Big slice; ~1–2 days of careful template/build-system work. Owns no functional change; pure structural reorg.
- **Strict-D-029-column bench slice (follow-up to D-031)** — if a measurement question demands distinguishing `bottomUpBoxesGPU(sceneBox)` from `bottomUpBoxesPartialGPU(huge)`, add a fifth column to the bench. The D-030 parallel symbols are already callable; the slice is just a new dispatch line + a new enum value + a new chart bar. Trigger: only if the D-031 chart shows GPU-side numbers in a regime where the depth-counter overhead crosses noise.
- **Alembic export slice (FR-013 / BDD-013)** — Q5 + Q6 resolved 2026-05-13 (target = Alembic Geometry Cache for Unreal import; eventual schema = OPolyMesh constant-topology, FaceSets per material slot, Y-up storage, velocity-attribute deferred; export FPS coupled to sim time-step). **Note 2026-05-14**: the originally-planned C-1..C-4 FlatBuffers-based intermediate (write FlatBuffers → byte-compare round-trip → Alembic) is **skipped** per user directive (FlatBuffers strength is reads, not writes). The Alembic export slice will go direct to Alembic library integration whenever it's picked up. `docs/design/mesh_animation_writer.md` stays on disk for reference but is not picked up as planned work.

`BDD-101` (the v1 spine: author → simulate → save/reload → export) is structurally complete except for the export step; once Alembic ships, BDD-101's acceptance can be mechanized.

## Recent scope changes

- **2026-05-14, B-2 Bullet RigidPhysicsBackend impl slice planned (this plan).** `feat/b-2-bullet-rigid-backend` branched off `main` HEAD `43ccb7f`. Vendors Bullet 3.25 as a git submodule at **`third_party/bullet3/`** (NOT `include/bullet3/` per the design doc — deviation rationale: matches the `third_party/imgui` precedent; `include/` is for ysim's own headers, not vendored source trees with ~50 MB of files). D-038 records the deviation. CMake sub-project via `add_subdirectory(third_party/bullet3 EXCLUDE_FROM_ALL SYSTEM)` after pre-setting 11 CACHE BOOL FORCE overrides (BUILD_BULLET2_DEMOS / BUILD_CPU_DEMOS / BUILD_EXTRAS / BUILD_OPENGL3_DEMOS / BUILD_UNIT_TESTS / INSTALL_LIBS / USE_GRAPHICAL_BENCHMARK / BUILD_SHARED_LIBS / BUILD_BULLET3 / USE_DOUBLE_PRECISION / BUILD_PYBULLET all OFF). New `include/BulletRigidPhysicsBackend.hpp` (class declaration with forward-declared Bullet types — isolates `btBulletDynamicsCommon.h`'s heavy compile cost to one TU) + `src/BulletRigidPhysicsBackend.cpp` (method bodies). Per-body storage triple `BulletBody{shape, motionState, body}` — declaration order is destruction-order-correct for Bullet's required cleanup order (body destroyed first, then motionState, then shape). Quat marshalling at boundary: ysim's `::Quat{w,x,y,z}` ↔ Bullet's `btQuaternion(x,y,z,w)`. Shape coverage: Box + Sphere + Plane translated; ConvexMesh + StaticMesh return `kInvalidBodyHandle` stub (B-2.1 / B-3 fills). Block 31 (2 clauses: sphere falls under gravity for one step ~2.7mm via Bullet semi-implicit Euler; sphere on static plane reaches resting contact at y ~ radius after 120 steps). **Block 30 relocated** from line ~9083 (AFTER Metal-less SKIP gate) to ~line 6161 (BEFORE SKIP gate, after the harness lambdas) — folds Estimator turn 33 WARNING; both Null AND Bullet contracts now run on Linux verify.sh. Self-test count 59 → 61. RIGID-BACKEND-PORTABILITY (D-037) now load-bearing across two backends (Null + Bullet). PRESERVED: Simulator template signature, ExplicitSystem::update Rigid dispatch (still no-op fall-through), GeneralMesh's lack of rigidBodyHandle — all three are B-3's surface. No new BDD/FR; D-038 only; no CM-NNN. Commit prefix `add:`. **User goal driver**: B-3 (next slice) wires Rigid behavior tag into Bullet and is the slice that closes the user's "Rigid bodies should fall under gravity" gap.

- **2026-05-14, B-1 RigidPhysicsBackend contract + Null impl slice shipped (commits `8a44275 add:` + `43ccb7f chore:` estimator turn 33).** D-037 — three new headers under `include/`: `Quat.hpp` (cut/paste from src/main.cpp:1554; struct body byte-identical), `RigidPhysicsTypes.hpp` (POD types in `namespace ysim::physics` — RigidShapeType enum + RigidShape + RigidInitial + BodyHandle alias + kInvalidBodyHandle = -1), `NullRigidPhysicsBackend.hpp` (12-method contract no-op impl; per-body initial snapshot via `std::vector<RigidInitial>`; kinematic step; CM-012 discipline — no exit() in helpers). Block 30 in `runSelfTest` (3 clauses: lifecycle / body-state-query / step-is-noop). Actual baseline at commit `186578d` was **56** (prior "57/57" reported by BDD-006 fix-turn was off-by-one in headline count; underlying clauses PASS). 56 → 59 self-test PASS deterministic across 5 runs. 4 bug-probes verified load-bearing + restored. New standing constraint **RIGID-BACKEND-PORTABILITY**. Estimator turn 33 returned WARNING (Block 30 sits after `runSelfTest`'s Metal-less SKIP gate at src/main.cpp:6164 → Linux container short-circuits before exercising the null contract) — folded into in-flight B-2 slice via Block 30 relocation.

- **2026-05-14, user directive — skip FlatBuffers mesh-cache slices.** User noted that FlatBuffers's strength is in *reads* (zero-copy random access on already-built buffers), not writes; the originally-planned C-1..C-4 write-pipeline slices are deferred indefinitely. Active order becomes `BDD-006 (done) → B-1 (done) → B-2 (current) → B-3 (next)`; Alembic export slice (when picked up) goes direct to the Alembic library, not via the FlatBuffers intermediate. `docs/design/mesh_animation_writer.md` stays on disk for reference but is not picked up as planned work. Memory at `~/.claude/projects/-Users-gyu-codes-ysim/memory/project_flatbuffers_caching_skipped.md` records this directive.

- **2026-05-14, user observation — Rigid bodies have no visible gravity effect.** Verified expected per BDD-006-RIGID-DISPATCH-PARKED standing constraint: `applyEnvironmentForces` (src/main.cpp:5034-5061) DOES accumulate gravity into Rigid-tagged meshes (mass×gravity, just like Cloth and Elastic), but `ExplicitSystem<METAL, PR>::update`'s integrator dispatch for `BehaviorType::Rigid` is a no-op fall-through (src/main.cpp:5891) — the externalForces array is never consumed by an integrator, so velocity/position never update. Rigid meshes appear static under gravity. Not a regression; expected behavior until B-3 wires `IRigidPhysicsBackend` (which handles gravity internally via Bullet). B-1 → B-2 → B-3 is the closure path; B-3 (next slice after B-2) ships the visible Rigid gravity behavior the user expects.

- **2026-05-06, persistence slice.** D-007 supersedes D-004: rotation moved from "JSON-only" to a real `GeneralMesh::rotationQuat` field after the Estimator BLOCKed the round-trip gap. No spec change — the design doc already required round-trip; the in-memory mirror was the missing piece. (See `docs/DECISIONS.md` D-004 / D-007.)
- **2026-05-06, persistence slice.** Design doc `docs/design/scene_format.md` updated to declare `"grid"` as the v1-shipped primitive shape with `"sphere"`/`"cube"` reserved-not-shipped, mirroring D-003. This was a doc/code reconciliation, not a scope change — v1 only ever had a grid initializer.
- **Pending in primitive-creation slice (this plan).** D-003 will be *amended* (not superseded) when `"sphere"` and `"cube"` ship as real initializers. The reserved-not-shipped *pattern* survives unchanged; only the membership of the reserved set narrows.
- **2026-05-06 / 07, primitive-creation slice.** D-010 amended D-003 (sphere/cube shipped). Two runtime bugs surfaced during manual GUI testing and were fixed in `2000aea` and `979f037`: CM-002 (`Scene::pack()` double-frees initializers when re-run because `meshes[i]` and `requestsGeneralMeshes[i]` share the pointer) and CM-003 (`BVH::build` skips re-allocation when primitive count changes, only checking `!tree.ptr`). Both fall in the gap the persistence slice's parked WARNING called out — the unit-test net cannot reach `Simulator::initialize` because of the GLFW/GL coupling in `mesh.initialize()`. This is the concrete motivation for promoting the test-harness slice to the next-after-environment-forces slot.
- **2026-05-07, env-forces slice (turn-2).** Estimator gave WARNING on the original commit (`4047af0`); user's manual GUI test caught CM-004 — both cloth force kernels (`compute_cloth_grid_forces_fast` and `compute_tri_spring_forces`) hardcoded gravity from `SimParams::G` and ignored the bound `externalForces` buffer. PLAN.md's "no Metal kernel changes" non-goal was based on a wrong assumption that the kernels consumed the buffer the C++ side bound; they did not. Patched in `e3a3154` (kernels now read `externalForces[id]` and drop the hardcoded gravity term). Pattern is now well-established: three successive slices ended with manual-test-catches-runtime-bug-in-Metal-path. The next planner-tracked milestone is the render-state decoupling slice, which is the structural prerequisite for actually testing these paths.
- **2026-05-07, render-state decoupling slice.** D-011 — `MeshGL<CPU>` lifted to `include/MeshGL.hpp`; `MeshRenderState` (in `include/MeshRenderState.hpp`) owns per-mesh GL state keyed by `mesh.id`; `GeneralMesh::meshGL` field removed. `Simulator::initialize` no longer touches GL. Estimator: NOTE-level. Strengthens `ARCHITECTURE §2.2/§2.3` boundary. Unblocks the harness slice (next milestone).
- **2026-05-07, harness slice (this plan).** Q-D resolved by Planner: headless Metal harness, **not** CPU backend reference. The CPU backend stays as type-system reservation only for v1. Resolution recorded in `docs/DECISIONS.md` D-012 when the harness slice landed.
- **2026-05-07, cloth-drape (BDD-007) slice.** Three of four BDD-007 clauses pass via Block 6 in `runSelfTest`. Real fix: uncommented `collisionPipeline.broadPhase.enlargeTrajectory(system.subh)` (CM-005 partial fix). New `cumulativeNarrowCollisions` counter in `Scene::packedCollisionData` so the harness can track contacts that fire and reset within a single frame. Tunneling clause still FAILs — parked under CM-005 for a future cloth-CCD slice (replace snapshot narrow-phase distance check with swept-segment-vs-triangle). Matrix row stays `warning`.
- **2026-05-07, BDD-002 import-mesh-ui slice.** `Simulator::importMesh` added with file-existence guard before `addFloatMesh` (without it, `MeshFileInitializer` silently queues a zero-vertex mesh on missing path — `ObjData::loadObject` is graceful on open-fail). `File > Import Mesh…` modal wired. Block 7 in `runSelfTest` mechanizes BDD-002's two "Then" clauses (happy: numMeshes++ + Float + path round-trip via `toSnapshot`; error: missing file → numMeshes unchanged). Block 8 (BDD-015) now resets the scene first because Block 7's imported mesh has a path that doesn't resolve relative to `/tmp/`. Estimator turn 4 verdict: WARNING (commit allowed) — two follow-ups bundled into the next (cloth-CCD) slice: tighten Block 7's happy-path assertion (AABB / facet-count); fix modal default path from `assets/Human.obj` to `src/assets/Human.obj` for the build-dir launch context.
- **2026-05-08, cloth-CCD slice (`feat/cloth-ccd` → `main`, commit `95a710f`).** D-013 — `narrow_pt_tri` rewritten as swept-segment CCD using new `xPrev` buffer (slot 10); writes signed distance (was abs'd before, which negated the integrator's push for tunneled particles). Per-substep `state.x → state.xPrev` snapshot in `Simulator::update`. Harness `subSteps = 4 → 8` for one-substep-lag headroom. Folded BDD-002 estimator turn-4 follow-ups (modal default + AABB/facet assertion). 16/16 self-test PASS; `verify.sh` exits 0 cleanly. CM-005 graduated to `OLD_MISTAKES.md` because the snapshot path was structurally removed. BDD-007 matrix row promoted `warning → pass`.
- **2026-05-08, doc maintenance pass.** `docs/CONVENTIONS.md` planned-status pointers refreshed (test/ + scripts/ exist; D-002 confirms doctest; include/ inventory updated). `docs/ARCHITECTURE.md §5` Q-D marked resolved by D-012. `docs/mistakes/COMMON_MISTAKES.md` CM-005 graduated; `docs/mistakes/OLD_MISTAKES.md` gained the "snapshot-only collision tests miss fast-moving thin geometry" pattern entry. PROJECT_STATE open-questions list synced with decisions (D-001/Q3, D-005/Q1, D-012/Q-D resolved). Role docs (`GENERATOR.md`, `PLANNER.md`, `ESTIMATOR.md`) codified learnings: spec-vs-label trap, Metal-kernel boundary check, fix-turns stay on slice branch (Generator); small-WARNING folding rule, escape-pattern detection, spec-substitution discipline (Planner); SKIP-vs-FAIL on Metal-less hosts, parked-failure callout, TESTS.md-not-labels (Estimator).
- **2026-05-07, translate-object slice planned (this plan).** `feat/translate-object` branched off `main` at `a85aba8`. BDD-003 closes via `GeneralMesh::transformPosition` field + `Simulator::translateObject` mutator + inspector `InputFloat3("Position")` + Block 9 in `runSelfTest`. Folded cloth-CCD turn-6 WARNING (`nparams.thickness = 0; // temp.` → `simulator.margin`) per small-WARNING bundling rule. Renderer mutates `state.x` directly rather than introducing a model-matrix refactor — that's a Non-goal explicitly carved out and recorded as the slice's load-bearing decision (new D-NNN). `xPrev` parity with `x` is load-bearing for D-013 swept-CCD; the slice mutates both by the same delta on translate.
- **2026-05-08, translate-object slice shipped (commits `ba1ef99` add + `53fe5a8` estimator).** D-014 — `GeneralMesh::transformPosition` + `Simulator::translateObject` (mutates `state.x` and `state.xPrev` by the delta; `state.v` unchanged). Inspector Transform header with `InputFloat3("Position")`; `IsItemDeactivatedAfterEdit` commit gate. `toSnapshot` reads `transformPosition` for realized meshes. Block 9 in `runSelfTest` mechanizes BDD-003's three "Then" clauses; 19/19 PASS. Profiler additions: `broad_enlarge_trajectory` profile scope; `FrameSnapshot::broad_collisions/narrow_collisions` per frame, exported in CSV between `fps` and section columns; `scripts/analyze_profile.py` writes a `<csv>_analysis/` directory. **Cloth-CCD WARNING partially folded:** `BruteForce::narrow` signature gained `thickness` parameter, but call site passes 0 — raising it exposed CM-006 (physics.metal vn-zero unconditional). Recorded; deferred to its own slice. Off-slice cleanup: reverted an uncommitted `refit() → enlargeTrajectory` swap that was hiding a BDD-007 regression independent of the slice.
- **2026-05-09, translate-pack + BDD-019 slice planned (this plan).** `fix/translate-pack-and-bdd019` branched off `main` at `53fe5a8`. Closes Estimator turn-7 WARNINGs: (a) `Simulator::translateObject` writes back to `mesh->initializer->params.center/offset` so re-pack reproduces the translate (currently silent drop on next `Scene::pack()` from create/import/load); (b) Block 10 in `runSelfTest` mechanizes BDD-019 against `docs/TESTS.md#BDD-019` verbatim — per-section timings updated, CSV export contains broad/narrow collision columns + recorded history, pause-gating Notes invariant. Block 9 gets a fourth "translate survives re-pack" assertion. CM-006 stays parked.
- **2026-05-09, translate-pack + BDD-019 slice shipped (commits `cf6101d` fix + `b0de64c` estimator turn 8).** D-015 — translateObject writes back to initializer center/offset; three-site cascade invariant (translateObject + Scene::pack + toSnapshot). Block 9 gained the round-trip clause (bug-probe-verified). Block 10 mechanizes BDD-019; CSV path substituted to `/tmp` for harness hygiene. BDD-019 matrix row promoted `pending → pass`. 23/23 self-test PASS. Estimator turn-8 WARNING (one item): Block 10's pause check is proxy-level — defers to next slice. CM-006 still parked.
- **2026-05-09, branch hygiene.** All 10 shipped slice branches (feat/cloth-ccd, feat/cloth-drape, feat/environment-forces, feat/import-mesh-ui, feat/persistence, feat/primitive-creation, feat/render-state-decoupling, feat/sim-self-test, feat/translate-object, feat/verify-and-load-warnings) plus the just-merged fix/translate-pack-and-bdd019 deleted via `git branch -d` (safe-delete, all merged). Pre-existing scratch branches (analyzelog, bvhexperiment, gui, guiexperiment, sh) preserved — predate the slice workflow.
- **2026-05-09, CM-006 vn-zero gate slice planned (this plan).** `fix/cloth-thickness-band` branched off `main` at `b0de64c`. Closes CM-006 (deferred two slices in a row): gate vn-zero in `integrate_cloth` and `integrate_cloth_grid` behind `(distance < thickness)` matching the position-push gate; wire `simulator.margin` to `narrowAndSortByVertices` from the call site. BDD-007 must continue to PASS — slice-critical acceptance gate. Estimator turn-8 WARNING (BDD-019 pause check) deliberately not folded — Metal-kernel risk + profiler refactor would expand surface; deferred to next slice. CM-006 graduates to OLD_MISTAKES.md after this slice. New D-016 records the kernel-side gate pairing as a load-bearing invariant.
- **2026-05-09, CM-006 slice shipped (commits `910075b` fix + `ea5e973` estimator turn 9).** D-016 — both cloth integrator kernels gate vn-zero AND position-push behind the same `(distance < thickness)` condition. Detection band (`radius + thickness`) stays asymmetrically wider, on purpose. Call site passes `simulator.margin` (was clamped to `0` for safety in the prior slice). CM-006 graduated to OLD_MISTAKES.md under a new high-level cause "contact-response gates fall out of sync when the detection rule changes." 23/23 self-test PASS deterministic across 5 consecutive runs. Estimator NOTE-clean; turn-8 WARNING (BDD-019 pause check proxy-level) still open and is the next slice's target.
- **2026-05-09, profiler pause-gate slice planned (this plan).** `fix/profiler-pause-gate` branched off `main` at `ea5e973`. Closes Estimator turn-8 WARNING by introducing `profiler::ProfilerFrameGate` RAII guard in `FrameProfiler.hpp` (~15 lines). Both production's render loop and Block 10 clause (c) construct the guard with the same predicate `!simulator.pause`; harness now drives the gate object directly, exercising the same predicate production uses. Pass label unchanged. No other refactor.
- **2026-05-09, profiler pause-gate slice shipped (commits `ed70e95` fix + `288b4a1` estimator turn 10).** D-017 — `profiler::ProfilerFrameGate` RAII guard in `FrameProfiler.hpp`. Production's render-loop begin/end pair replaced with a single guard + explicit `close()` before the window-title read. Block 10 clause (c) constructs the guard with `!sim.pause`; bug-probe-verified (flipping to hardcoded `true` makes clause (c) FAIL with "ProfilerFrameGate(collect=false) still pushed a snapshot"). 23/23 PASS, Estimator NOTE-clean.
- **2026-05-09, BDD-102 determinism slice planned (this plan).** `feat/bdd-102-determinism` branched off `main` at `288b4a1`. New Block 11 in `runSelfTest` does two-run bit-identical compare of `state.x` + `state.v` after pumping 30 frames of `buildSyntheticScene`. Spec-substitution: state buffers stand in for Alembic outputs (FR-013 blocked on Q5/Q6); same-binary-same-machine runs have no compiler-reordering ULP drift, so bit equality is the tightest informative tolerance. If Block 11 reports drift, the cause is real nondeterminism (atomic ordering in `narrow_pt_tri` is the prime suspect) and the slice stops + hands back to Planner — don't loosen tolerance to make it pass.
- **2026-05-09, BDD-102 first Generator turn discovered CM-007 (uncommitted, BLOCKed).** Block 11 mechanization went in cleanly but FAILed at byte 0 of state.x — `divergence origin: init`. Root cause: `MeshGridInitializer::initialize` (line ~1063) uses `rand()/RAND_MAX/10000.f` for jiggle; `Simulator::addCloth` hardcodes `jiggle = true`; global RNG state advances across two `buildSyntheticScene` calls so the second cloth gets a different jiggle sequence. Generator converted FAIL → SKIP per "stop and hand back" instruction; Estimator turn 11 BLOCKed because SKIP-on-mismatch suppresses the BDD-102 signal. Estimator's earlier 4 code-review points (SKIP→FAIL, per-frame compare, fail-loud on null buffers, positions-only) are folded into the fix-turn's plan. CM-007 active in COMMON_MISTAKES with three fix-direction options.
- **2026-05-09, BDD-102 fix-turn planned (this plan).** Same branch `feat/bdd-102-determinism`, commit prefix `fix:` per Generator role doc (BLOCK fix-turns stay on slice branch). Closes the BLOCK by fixing CM-007 directly via per-mesh seeded `std::mt19937` in `MeshGridInitializer` (option a), seeded from `mesh.id` so save/load reproduces jiggle for free (no scene-format change). Block 11 cleanup per Estimator's 4 review points + matrix row promotion + CM-007 graduation. New D-018 records the seeding decision.
- **2026-05-09, BDD-102 fix-turn shipped (commits `315d9ff` fix + `992b658` estimator turn 12).** D-018 — per-mesh seeded `std::mt19937` from `mesh.id` (Scene::numMeshes pre-call in addCloth; o.id in loadScene). No scene-format change. Block 11 reworked: SKIP→FAIL on mismatch, per-frame compare, fail-loud on null buffers, positions-only (state.v dropped). 24/24 PASS deterministic across 5 runs; bug-probe verified. CM-007 graduated to OLD_MISTAKES.md. Estimator turn 12 WARNING-only (BDD-102 vs Alembic-bytes substitution acknowledged as long-standing open).
- **2026-05-09, BDD-004 quaternion slice planned (this plan).** `feat/bdd-004-rotation` branched off `main` at `992b658`. Pure CPU + persistence test mechanizing BDD-004's "Then" clauses: compose rotations across save/load boundary, assert orientation match within 1e-5 + unit-norm at each step. Forces the bare `Quat` struct to gain Hamilton-product + normalize/norm helpers (~30 lines) — these become canonical for future rotation consumers. No UI, no renderer change, no `Simulator::rotateObject` API (defer to its own slice). New D-019 will record the math + Hamilton convention.
- **2026-05-09, BDD-004 slice shipped (commits `ea4d1e4` add + `089279b` estimator turn 13).** D-019 — three free functions next to `struct Quat` (~line 1554): `operator*` (Hamilton, `a * b = apply b first then a`), `quatNorm`, `quatNormalize` (with 1e-12 degenerate guard returning identity). Block 12 in `runSelfTest` mechanizes BDD-004 verbatim with bug-probe-verified diagnostics. 25/25 PASS deterministic. Matrix row promoted. Estimator turn 13 WARNING: Block 12 didn't assert `r1_post_load` is unit-norm before re-multiplying — folded into next slice as ~3-line addition. Plus a NOTE about q vs -q sign equivalence (informational, no action needed unless persistence ever canonicalizes signs).
- **2026-05-09, BDD-010 collision-detected slice planned (this plan).** `feat/bdd-010-collision-detected` branched off `main` at `089279b`. Block 13 in `runSelfTest` mechanizes BDD-010's two clauses: positive (cloth + ground at touching distance → ≥1 narrow with distinct mesh ids in the (A, B) pair); negative (cloth far above ground → 0 narrow). Single `sim.update()` per scene; `cumulativeNarrowCollisions` is the "ever fired" signal. Self-collision parked per PRD §4 — handled by default `enableSelfCollisions = false` + `query != target` filter in the assertion. Folds in BDD-004 turn-13 WARNING as small follow-up (~3 lines). No new D-NNN — mechanization-only.
- **2026-05-09, BDD-010 slice shipped (commits `f9dbc9b` add + `ea6cc48` estimator turn 14).** Block 13 mechanized; build-time discovery: cloth at y=-0.99 didn't trigger broad-phase due to flat-AABB-no-velocity-inflation at t=0; co-located at y=-1.0 fixed it. BDD-004 unit-norm fold-in landed in Block 12. Bug-probe verified for both clauses. 27/27 PASS. Estimator NOTE-clean.
- **2026-05-09, BDD-017 ray-pick slice planned (this plan).** `feat/bdd-017-ray-pick` branched off `main` at `ea6cc48`. Block 14 in `runSelfTest` mechanizes BDD-017's two clauses (non-overlapping + overlapping front-most-wins) using the existing `BroadPhase::queryClickRay` + `clickRayCollisions` arrays — no API extraction needed. Spec-substitution: harness skips camera unprojection and constructs world-space rays directly. Pure mechanization slice; no new D-NNN.
- **2026-05-10, BDD-017 ray-pick slice shipped (commits `eb496d0` add + `3e5f6a4` estimator turn 15).** Block 14 mechanizes BDD-017 with both clauses; build-time discovery of CM-008 (`BroadPhase::build` skips Float-mesh per-mesh BVH rebuild, harness scene-swaps reuse stale data) → workaround `sim.collisionPipeline.broadPhase.objTrees.clear()` between scenes. CM-008 added to active list with both harness fix and production-side fix-direction. 29/29 self-test PASS deterministic. Estimator turn 15 WARNING-only, two coverage gaps folded into next slice.
- **2026-05-10, BDD-017 coverage fix-turn planned (this plan).** `fix/bdd-017-coverage` branched off `main` at `3e5f6a4`. Closes Estimator turn-15's two WARNINGs: (1) overlapping clause walks `clickRayCollisions[]` for both-cubes-present check (the existing `numHits >= 2` is satisfiable by a single cube), (2) both clauses write `pickClosest()` into `sim.selectedObj` and assert from there, mirroring production's `selectedObj = closestObj` at `src/main.cpp:6718`. ~10 lines net change. Pass labels stay unchanged. No new D-NNN; CM-008 production-side fix still deferred.
- **2026-05-10, BDD-017 fix-turn shipped (commits `d593f2b` fix + `9f368b2` estimator turn 16).** Both turn-15 WARNINGs closed. **Unplanned scope expansion:** the both-cubes-present check exposed a real production BVH bug. `BVH::queryClickRay`'s leaf branch fell through to `if(node.childB > 0) queryClickRay(ray, tree[node.childB])`, treating the leaf's primitive id as a node index → recursion exploded within cubeFront's tree, filled the 4096-entry `clickRayCollisions` buffer, starved cubeBack. Production was masked because smallest-tmin walk still returned the right id. Fixed with `return;` after leaf-write. D-020 records the BVH leaf-return invariant; CM-009 records the "mixed leaf/interior semantics in BVH walks" trap pattern. Estimator turn 16 NOTE-clean (with informational note on the scope expansion).
- **2026-05-10, role-doc maintenance pass (commit `4217619`).** Each role updates its own `docs/roles/<ROLE>.md` from the role's perspective with discipline learned from BDD-102 / BDD-004 / BDD-019 / BDD-017 / D-020. PLANNER gains procedure step 7 (stricter-than-spec assertions when cost is near-zero) + step 8 (architectural invariant propagation) + spec-substitution log. GENERATOR gains bug-probe discipline + build-time-discoveries decision tree (fix-on-the-way OR hand back) + stable harness gotchas (CWD trap, `objTrees.clear()`, `sim.update()` for BVH refit, etc.). ESTIMATOR gains "SKIP-as-suppression is BLOCK-worthy" + "stricter-than-spec assertions are valuable signal" + "standing structural WARNINGs" + "trust the diff over the green checkmark."
- **2026-05-10, FR-004 UI rotate-object slice planned (this plan).** `feat/rotate-object` branched off `main` at `4217619`. `Simulator::rotateObject(meshId, newQuat)` mirrors D-014's translateObject — rotates `state.x` and `state.xPrev` around `mesh.transformPosition` pivot, composes onto `mesh.rotationQuat`. Two new Quat helpers (`quatConjugate`, `rotateVector`) extend D-019's family (D-022). Inspector wiring uses raw `float*` to avoid header coupling. Block 15 mechanizes rotate-then-step on a Float cube. Pack-roundtrip explicitly deferred to its own slice (rotate analog of D-015) — the Planner design call about auto-calling `applyPendingMaterials()` from `Simulator::initialize()` is what makes that scope Planner-tier. New D-021 / D-022.
- **2026-05-10, FR-004 UI rotate-object slice shipped (commits `d550f82` add + `9c8da75` estimator turn 17).** D-021 + D-022 landed; Block 15 mechanizes rotate-then-step (Float cube witness vertex (0.25, -0.25, -0.25) → (0.25, 0.25, -0.25) under 90°-Z). 31/31 self-test PASS deterministic. Estimator turn 17 WARNING — translateObject/rotateObject don't refit the BVH; click-pick on paused sim hits old pose. Folded into next fix-turn slice.
- **2026-05-10, refit-after-edit fix-turn planned (this plan).** `fix/refit-after-edit` branched off `main` at `9c8da75`. Closes turn-17 WARNING by calling `collisionPipeline.broadPhase.refit()` after the state.x mutation in both `translateObject` and `rotateObject` (one line each). `BroadPhase::refit()` covers both per-mesh refit and SCENE-level rebuild in one call. Block 16 mechanizes the invariant for both functions: translate-then-pick (new pos hits, old pos misses) and rotate-then-pick (45°-Z grows AABB from ±0.25 to ±0.354; witness ray at x=0.30 misses pre-rotate, hits post-rotate). New D-023 records the refit-after-direct-state.x-mutation invariant.
- **2026-05-10, triangle-precision click-pick slice planned (this plan).** Will branch off main once refit-after-edit merges. **User-reported regression:** rotating a Plane (`addGround`) to a non-axis-aligned pose (quat normalized from `(1, 2, 0, 0)`) now steals every click because click-pick ranks by leaf-AABB-tmin, not actual triangle-tmin. The Plane's tilted leaf AABB envelops the camera frustum, so its AABB-tmin is always smaller than other objects'. **Forensic finding:** latent structural bug since the file's history — `queryClickRay`'s leaf has always written `node.aabb.intersect`'s tmin (`src/main.cpp:3843`), not a triangle-vs-ray tmin. For axis-aligned scenes the AABB approximation was tight enough; D-021 + D-023 just made tilted poses reachable. **No specific commit "broke" click-pick.** Fix: two-stage walk via new `Simulator::pickClosestTriangleHit(ray)` helper that runs Möller–Trumbore on each `clickRayCollisions[]` candidate. Production callback + harness Block 14 share the helper. Block 17 reproduces the user's exact scenario. New D-024 + CM-010.
- **2026-05-10, refit-after-edit fix-turn shipped (commit `ab0cae4`).** Closed Estimator turn-17 WARNING with a one-line `collisionPipeline.broadPhase.refit()` call after state.x mutation in both `translateObject` and `rotateObject`. Block 16 mechanizes both translate-then-pick and rotate-then-pick invariants (45°-Z grows AABB from ±0.25 to ±0.354; witness ray at x=0.30 misses pre-rotate, hits post-rotate; bug-probe verified by removing the refit calls). New D-023 records the refit-after-direct-state.x-mutation invariant. 32/32 self-test PASS. Discovered during manual GUI testing: a deeper triangle-precision regression — addressed by the next slice.
- **2026-05-10, triangle-precision click-pick slice shipped (commits `bac733c` fix + `d54cb44` estimator turn 18).** D-024 — `BVH::queryClickRay`'s leaf branch does ray-vs-triangle (Möller–Trumbore, epsilon 1e-6) using the existing `positions.ptr`/`primitives.ptr` slices into mesh storage. The interior-node AABB filter stays for cheap traversal pruning, but the value written to `clickRayCollisions[]` is the triangle's actual `t`, so consumers' smallest-tmin walk is automatically triangle-precise. **No consumer-side helper, no two-stage walk** — the BVH produces triangle hits directly. User's intuition ("just query ray to BVH") was correct. Block 17 reproduces the user's discriminating scene (size-30 ground tilted 60°-X around y=-1 pivot — Plane AABB-tmin = 2.5 vs cube 9.75; triangle-tmin = 10.577 loses to cube 9.75). Bug-probe verified — writing `hit.tmin/tmax` instead of `triT, triT` makes Block 17 FAIL with `tilted ground stole the click`. CM-010 records the trap pattern for future BVH variants. 34/34 self-test PASS deterministic. Estimator turn 18 WARNING: BDD-017 row's test address didn't cross-reference Block 17 — folded into the next slice.
- **2026-05-10, rotate pack-roundtrip slice planned (this plan).** `feat/rotate-pack-roundtrip` branched off `main` at `d54cb44`. Closes the rotate analog of D-015's translate pack-roundtrip — the gap that's been deferred 5 slices in a row (PLANNER.md procedure step 6 escape-pattern detection forced action). Design call: Shape A (initializer carries rotation, mirroring D-015) vs Shape B (`Simulator::initialize()` auto-calls `applyPendingMaterials()`; `pendingRotations` side-table populated by `rotateObject`; `applyPendingMaterials` calls `rotateObject` per saved entry instead of just setting rotationQuat). **Picked Shape B** for ~5 lines vs Shape A's 4-class refactor. Block 18 mechanizes the round-trip; Estimator turn-18 WARNING folded as `docs/TEST_MATRIX.md` BDD-017 row gaining a Block 17 cross-reference (~1 line). New D-025.
- **2026-05-10, rotate pack-roundtrip slice shipped (commits `1650753` fix + `7d45c6b` estimator turn 19).** D-025 landed as Shape B; three production-side touchpoints in `Simulator::rotateObject` (writes `pendingRotations[meshId] = newAbs` after state.x mutation + refit), `Simulator::initialize` (auto-calls `applyPendingMaterials()` at the end), and `Simulator::applyPendingMaterials` (snapshots `pendingRotations` before iterating, then calls `rotateObject(id, q)` per entry — snapshot is load-bearing because rotateObject re-populates the map during its call). Block 18 mechanizes 90deg-Z rotate cube → addCube (forces re-pack) → assert state.x[0] still rotated within 1e-5. Pass label `FR-004 / rotateObject survives Scene::pack rebuild`. Bug-probe verified — skipping the pendingRotations write makes Block 18 FAIL with `expected (0.25, 0.25, -0.25) got (0.25, -0.25, -0.25)`. Estimator turn-18 WARNING fold-in landed (BDD-017 row Block 17 cross-reference). 35/35 self-test PASS deterministic across 5 runs. Estimator turn 19 NOTE-clean (only the standing Metal-less-host SKIP note). 5-slice deferral chain ended.
- **2026-05-10, CM-008 production-side fix slice planned (this plan).** `fix/cm-008-broadphase-skip` branched off `main` at `7d45c6b`. Closes the long-deferred CM-008: `BroadPhase::build` (`src/main.cpp:3989`) skips per-mesh BVH rebuild when the previous slot's `objBehavior == Float`, but doesn't check whether it's the same mesh. After `resetScene` + new addCube×N at the same numMeshes count, the stale tree wins. Harness has carried 7 `objTrees.clear()` workaround sites. Design call: Shape A (lifetimeId field — never-resetting monotone counter on Scene; `mesh.lifetimeId` set at addGeneralMesh time; `TRI_LBVH::builtForLifetimeId` cached at build; gate skip on lifetimeId match) vs Shape B (explicit `BroadPhase::invalidate()` API requiring caller discipline). **Picked Shape A** for "foolproof beats explicit." `mesh.id` keeps its current numMeshes-derived semantic (D-018 RNG seed); `lifetimeId` is a separate field. Block 19 mechanizes the scene-swap-at-same-count pattern WITHOUT manually clearing objTrees; the 7 workaround sites are removed. CM-008 graduates to OLD_MISTAKES.md. New D-026.
- **2026-05-10, CM-008 production-side fix slice shipped (commits `668dc4f` fix + `de83164` estimator turn 20).** D-026 landed as Shape A; `Scene<BE,PR>::lifetimeMeshCount` (never-resetting monotone counter), `mesh.lifetimeId` (set at addGeneralMesh time, mirrored in move constructor — load-bearing for move-realized meshes), `TRI_LBVH::builtForLifetimeId` cached at build; `BroadPhase::build` Float-mesh skip gains the `builtForLifetimeId == scene.meshes[i].lifetimeId` conjunct. All 7 harness `objTrees.clear()` workaround sites removed. CM-008 graduated to OLD_MISTAKES.md under new high-level cause "skip optimizations silently inherit prior-iteration identity when slot indices align." Block 19 mechanizes the scene-swap-at-same-count round-trip; pass label `CM-008 / scene-swap-at-same-count rebuilds Float-mesh BVH`. **Bug-probe cluster**: reverting the lifetimeId clause produces 5 simultaneous FAILs (Block 19 + Blocks 14a/14b/16a/16b/17/18 whose workarounds were removed and now legitimately depend on the production fix) — the cluster of FAILs is the load-bearing proof that the fix is doing real cross-slice work, not just satisfying its own narrowly-scoped assertion. 36/36 self-test PASS deterministic across 5 runs. Estimator turn 20 NOTE: BDD-017 row prose at `docs/TEST_MATRIX.md:31` still references the removed `objTrees.clear()` workaround — folded into next slice.
- **2026-05-11, FR-005 material-edit data-layer subset slice planned (this plan).** `feat/material-inspector-data-layer` branched off `main` at `de83164`. Closes BDD-005's data-layer "Then" clauses while explicitly parking the renderer-side "preview render reflects the lower roughness" clause as documented gap (PBR-preview-shader slice not yet planned). Design call: Shape B mutator `Simulator::setMaterial(meshId, Material)` over Shape A direct mutation. Mutator mirrors translateObject (D-014) and rotateObject (D-021) shape — find-mesh + mutate-live `mesh->material = mat` + write `pendingMaterials[meshId] = mat` for re-pack survival via D-025's auto-applyPendingMaterials. Inspector wiring: `MeshInspectorTarget` gains 4 new pointer fields (metallic, roughness, specularWeight, emissionColor) + on_material_edit callback; 5 ImGui widgets fire `simulator.setMaterial(id, m)` on every change (no commit-on-release gate — material edits are cheap CPU memcpy, unlike translate's broadPhase.refit). Block 20 mechanizes 4 pass clauses: setMaterial writes all 5 fields, sim step preserves (BDD-103 backend-boundary), save+load round-trips, addCube re-pack preserves (D-025 sister). Matrix row BDD-005 promotes `pending → warning` (renderer clause parked under documented-gap convention). Estimator turn-20 NOTE folded: BDD-017 row prose at line 31 drops stale `objTrees.clear()` reference, replaces with D-026 citation (~1 line). Clamping happens at slider widget range, not in setMaterial (philosophical alignment with D-005's data-layer-is-authority stance). New D-027.
- **2026-05-11, FR-005 data-layer slice shipped (commits `a252cf3` add + `2c33599` estimator turn 21).** D-027 landed; `Simulator::setMaterial(meshId, Material)` mutator added adjacent to translateObject / rotateObject; `MeshInspectorTarget` extended with 4 new pointer fields + on_material_edit callback; `src/mesh_inspector_gui.cpp` renders 2 ColorEdit3 + 3 SliderFloat (0..1) widgets via a `fireMaterialEdit` lambda. Block 20 mechanizes 4 pass clauses; 40/40 self-test PASS deterministic across 5 runs. Two bug-probes verified (skip pendingMaterials write → Phase 4 FAIL; skip setMaterial call → Phases 1/2/3 FAIL). Matrix row BDD-005 promoted `pending → warning`. Estimator turn 21 returned **WARNING** with two items: (1) BDD-005 intentionally `warning` (parked render clause — planned); (2) skill infrastructure changes outside FR-005 plan (caught silent scope expansion). Item 2 was split into its own chore commit during slice close-out.
- **2026-05-11, skill workflow infrastructure chore shipped (commit `9b76f7b`).** Renamed `.claude/skills/{Planner,Generator,Estimator}` → lowercase variants; added `.claude/skills/slice/SKILL.md` — composite skill orchestrating the full Planner → Generator → Estimator cycle in one invocation. Halts on State (C) "Estimator hasn't run" or State (E) "mixed working tree" rather than improvising destructive recovery. Estimator independence preserved by always invoking via `Agent(codex:codex-rescue)`, never via local `Skill(estimator)`. Reduces manual-step burden of the 3-role workflow from ~6 messages per cycle to 1–2 (user invokes `/slice` and optionally `/codex:rescue` between).
- **2026-05-11, PBR preview shader slice planned (this plan).** `feat/pbr-preview-shader` branched off `main` at `9b76f7b`. Closes BDD-005's renderer-side "Then" clause parked since FR-005 data-layer subset (D-027). Current fragment shader at `src/shader/shader.frag` uses Phong with a point light and consumes only `mesh.material.baseColor`; this slice rewrites it as a GGX-Smith microfacet PBR with a single directional light + small ambient, plumbs all 5 D-005 material uniforms through `MeshGL::draw`'s extended signature. Design call (D-028): GGX-Smith over Cook-Torrance Beckmann and Lambert+specular (anchors to OpenPBR; only GGX gives visible roughness change). Single directional light over point light (DCC default; no falloff anomalies). Individual uniforms over UBO (5-field count is stable in v1; compile-time call-site safety). Manual-test mechanization over FBO pixel-diff and CPU reference BRDF (harness has no GL context; ~150-line harness GL expansion to add one is its own slice — added to candidates list). Matrix row BDD-005 stays `warning` — render-clause correctness is user's visual gate. **Standing structural WARNING introduced** (parallel to BDD-102's Alembic-bytes substitution) so future Estimator turns don't re-flag. No new self-test Block. User runs GUI manual test (drag roughness slider 0.1 → 0.9, confirm specular highlight broadens) before `/codex:rescue`. New D-028.
- **2026-05-11, PBR preview shader slice shipped (commits `098c37d` add + `eb1413e` estimator turn 22).** D-028 landed: GGX-Smith microfacet (`fresnelSchlick` + `distributionGGX` + `geometrySmith`) + single directional light + small constant ambient (0.10 * baseColor); 5 D-005 uniforms plumbed through `MeshGL::draw`'s extended signature; old Phong path removed. `SceneEnvironment` gained `lightColor` + `lightIntensity` fields (in-memory only, not persisted) with Environment-panel widgets. Initial tuning was too dim — fixed via direct-radiance upload (C++ multiplies `lightColor * lightIntensity` before upload, shader has no /N rescale) + ambient bumped 0.03 → 0.10. User's manual visual gate confirmed roughness change produces visibly broader specular highlight at high roughness. 40/40 self-test PASS deterministic. Estimator turn 22 WARNING was environmental (Codex sandbox `$HOME` can't write clang module cache; passes with `HOME=/tmp`). NOTE about bvh.metal whitespace was user's WIP for next slice — stashed during close-out, restored on the BVH slice branch.
- **2026-05-14, user directive — skip FlatBuffers mesh-cache slices.** User noted that FlatBuffers's strength is in *reads* (zero-copy random access on already-built buffers), not writes; the originally-planned C-1..C-4 write-pipeline slices are deferred indefinitely. Active order becomes `BDD-006 (done) → B-1 (this) → B-2 → B-3`; Alembic export slice (when picked up) goes direct to the Alembic library, not via the FlatBuffers intermediate. `docs/design/mesh_animation_writer.md` stays on disk for reference but is not picked up as planned work. Memory at `~/.claude/projects/-Users-gyu-codes-ysim/memory/project_flatbuffers_caching_skipped.md` records this directive.

- **2026-05-14, user observation — Rigid bodies have no visible gravity effect.** Verified expected per BDD-006-RIGID-DISPATCH-PARKED standing constraint: `applyEnvironmentForces` (src/main.cpp:5034-5061) DOES accumulate gravity into Rigid-tagged meshes (mass×gravity, just like Cloth and Elastic), but `ExplicitSystem<METAL, PR>::update`'s integrator dispatch for `BehaviorType::Rigid` is a no-op fall-through (src/main.cpp:5891) — the externalForces array is never consumed by an integrator, so velocity/position never update. Rigid meshes appear static under gravity. Not a regression; expected behavior until B-3 wires `IRigidPhysicsBackend` (which handles gravity internally via Bullet). B-1 → B-2 → B-3 is the closure path; B-1 (this slice) ships the contract foundation.

- **2026-05-14, B-1 RigidPhysicsBackend contract + Null impl slice planned (this plan).** `feat/b-1-rigid-physics-backend-contract` branched off local `main` HEAD (`186578d`). Foundation slice per `docs/design/rigid_physics_backend.md` §B-1. Three new headers: `include/Quat.hpp` (cut/paste from src/main.cpp:1554; struct body unchanged), `include/RigidPhysicsTypes.hpp` (POD types in `namespace ysim::physics`: RigidShapeType enum + RigidShape + RigidInitial + BodyHandle alias + kInvalidBodyHandle), `include/NullRigidPhysicsBackend.hpp` (12-method contract no-op impl; per-body initial snapshot via `std::vector<RigidInitial>`). Block 30 in `runSelfTest` (placed OUTSIDE the Metal gate — pure C++) exercises the contract surface with 3 clauses (lifecycle / body-state-query / step-is-noop) per D-037; 57 → 60 self-test count. **PRESERVED**: Simulator template signature unchanged (no 4th `RigidBackend` template parameter yet — B-3's surface change); ExplicitSystem::update Rigid dispatch unchanged (still no-op fall-through; B-3 wires); GeneralMesh has no rigidBodyHandle field yet (B-3); applyEnvironmentForces unchanged. The Quat extraction is the minimum necessary scope-expansion (forward-decl alone fails because `RigidInitial` holds Quat by value; brace-init defaults need complete type). Helpers (`quatNormalize`, `operator*`, `quatFromAxisAngle`, etc.) stay in main.cpp. New standing constraint **RIGID-BACKEND-PORTABILITY** (any contract change must update every backend in same commit). 4 bug-probes (a/b/c/d) cover handle / position / step-noop / rotation. Commit prefix `add:`. No new BDD/FR; no CM-NNN. BDD-008 row stays `pending` (Null backend is kinematic; B-3 promotes it).

- **2026-05-13, BDD-006 Behavior Assignment UI slice planned (this plan).** `feat/bdd-006-behavior-assignment-ui` branched off `main` at `037d83f` (post-design-docs chore). Mechanizes BDD-006 via Q2's TWO-entry dropdown resolution (`TriangularCloth` default + `FastGridCloth` grid-only). Design call D-036: in-place behavior switch via `Simulator::changeBehavior(meshId, newType)` (preserves mesh.id + transform + shape; rebuilds behavior state). Grid-only enforced both widget-side (combo entry conditional) AND setter-side. New `on_behavior_change` callback + `behavior_type` int* pointer + `grid_eligible` bool on `MeshInspectorTarget` (parallel-symbol additions; existing 9 fields unchanged). Block 29 mechanizes 3 pass clauses (Float→TriangularCloth + TriangularCloth→Float + FastGridCloth grid-only setter-reject). 51 → 54 self-test count. Spec substitution: Rigid dispatch parked behind slice B-3 → new BDD-006-RIGID-DISPATCH-PARKED standing constraint; BDD-018-BEHAVIOR-TAG-PARKED narrows to "Rigid only." Reserved-not-shipped behaviors absent from dropdown + rejected by setter. New D-036; no new CM-NNN.

- **2026-05-13, role-doc maintenance pass planned (this plan).** `chore/role-doc-maintenance-pass` branched off `main` at `282780e`. Folds 5 discipline learnings from slices 17–30 into `docs/roles/{PLANNER,GENERATOR,ESTIMATOR}.md`: PROBE-COVERAGE-EDGES (D-035 turn-30 antipode miss), PARALLEL-IMPL-LOCKSTEP (D-035 lockstep across main.cpp + MeshInspectorWindow.hpp — canonicalized into a new "Standing constraints" subsection in PLANNER.md), BLOCK-FIX-TURN cadence (Planner-side procedure for fix-turns), UTILITY-HELPER-EXIT-IS-CALLERS-DECISION (D-034/CM-012 procedural prompt), Standing-fold-in candidates list canonicalized (consolidates DUPLICATED-INSPECTOR-WIRING, GLFWINIT-NON-REF-COUNTED, BDD-018-BEHAVIOR-TAG-PARKED, BDD-102-vs-ALEMBIC-BYTES into one home). Pure docs slice; no code/test changes; commit prefix `chore:`. Special-case write-set extension: Generator authors all three role docs (same convention as prior maintenance pass `4217619`). No new D-NNN, CM-NNN, BDD/FR; no matrix changes. Pacing aligns with established "every 10–15 cycles" rhythm (last pass was 14 cycles ago).

- **2026-05-13, inspector rotation ergonomics slice planned (this plan).** `feat/inspector-rotation-ergonomics` branched off `main` at `24c47ac`. Mechanizes FR-004 Notes ("UI may expose Euler/axis-angle as input affordances"). Design call: Shape B — inspector converts in-widget to Quat then fires the existing `on_rotate` callback; `MeshInspectorTarget` surface untouched. New free-function helpers next to D-019's quaternion family (`src/main.cpp:~1605`): `quatFromAxisAngle`, `quatToAxisAngle`, `quatFromEulerXYZ`, `quatToEulerXYZ`. `MeshInspectorWindowState` gains `int rotation_input_mode` field (0=Quat default, 1=Euler XYZ, 2=AxisAngle). Rotation panel in `mesh_inspector_gui.cpp` gains a 3-mode toggle; each mode reads `*target.rotation_wxyz`, renders the appropriate widget, on commit converts to Quat and fires `target.on_rotate`. New Block 28 in `runSelfTest` asserts round-trip math correctness within `1e-5` for non-singular inputs; 49 → 51 self-test count. Folds turn-29 NOTE-1: tighten `Program::cleanUp()` to also zero `tessControlShaderID` and `tessEvalShaderID`. New D-035 records the conversion conventions (Euler XYZ intrinsic Tait-Bryan = Blender's; axis-angle output in [0, π] with auto-normalized axis; gimbal-lock fallback lossy by design). Widget rendering manual-test-only — the harness gate covers only the math layer; Block 26 (BDD-018) already covers the propagation seam. No new BDD/FR; no matrix row changes.

- **2026-05-13, loadshader skip-safety + HiddenGLContext cleanup slice planned (this plan).** `fix/loadshader-skip-safety` branched off `main` at `1d8b6d9`. Closes Estimator turn-27 WARNING (`Program::loadShader` exit-on-failure defeats Block 25's documented SKIP semantic) + NOTE (`HiddenGLContext` destructor doesn't call `glfwTerminate()` on early-init failure paths). Design call: Shape B from the slice brief — `printLog()` and `linkShader()` both stop calling `exit(1)`; `linkShader` calls `cleanUp()` after `printLog()` on link-status failure; each `loadShader` overload gets an early-return after each `loadShaderOf` to avoid wasteful subsequent calls. After the fix, `programID == 0` honestly signals load failure. `HiddenGLContext` gains `bool glfwInitialized = false` field; constructor sets it post-`glfwInit`-success; destructor calls `glfwTerminate()` if true. Production caller in `main.cpp` gets defensive `if (!programID) std::exit(1)` to preserve loud-failure behavior; harness gets clean SKIP. New Block 27 mechanizes D-034 (invoke `loadShader` with bogus filenames + assert `programID == 0`); 48 → 49 self-test count. Bug-probe (a) loader: temporarily revert the `printLog` change → self-test aborts inside Block 27, proves load-bearing. HiddenGLContext destructor verified by visual review only (runtime probe requires forcing glfwInit failure mid-test, not practical). New D-034 (loader failure contract) + new CM-012 (utility-helper-calls-exit trap pattern). No new BDD/FR. No matrix row changes. Parallel-symbol rule preserved in its softer form: modifications are in-place edits of existing methods per the user's "refactor" + "add to destructor" language; new symbols are the `glfwInitialized` field, Block 27, D-034, CM-012.

- **2026-05-13, BDD-018 inspector live-edit propagation slice planned (this plan).** `feat/bdd-018-inspector-live-edit` branched off `main` at `01092b9`. Closes pending BDD-018 (matrix row 32) by mechanizing "Inspector edits propagate live." Production wiring at `src/main.cpp:8349-8387` already builds `mesh_inspector::MeshInspectorTarget` per frame and routes 3 implemented edit paths into the simulator: D-027 `setMaterial`, D-014 `translateObject`, D-021 `rotateObject`. Mechanization shape (option b from slice brief): Block 26 in `runSelfTest` constructs `MeshInspectorTarget` matching production shape exactly, invokes each callback with synthetic edit values, asserts exactly-one-setter-call (stricter-than-spec per PLANNER.md step 7) + live in-place pointer mutation + sim-step survival without `simulator.initialize()`. Three pass clauses (material/translate/rotate); 45 → 48 self-test count. Spec substitutions documented: behavior-tag clause parked under BDD-006/Q2 blocked; "next rendered frame" mechanized as in-place pointer value the renderer reads each frame (no FBO needed — the BDD-018 load-bearing claim is on the *input* data path, not the *output* pixels). Parallel-symbol rule: no `MeshInspectorTarget` widening, no `mesh_inspector_gui.cpp` modification, no production-side helper extraction. Turn-27 WARNING (`Program::loadShader` skip-safety) + NOTE (`HiddenGLContext` destructor cleanup) explicitly NOT folded per user direction — queued for a future small fix slice. No new D-NNN / CM-NNN — pure mechanization.

- **2026-05-12, FBO-based render harness slice planned (this plan).** `feat/fbo-render-harness` branched off `main` at `bb3b667`. Closes BDD-005's standing structural WARNING (render clause parked manual-test-only when D-028 PBR preview shader shipped). Design call (D-032): new `include/HiddenGLContext.hpp` (parallel symbol to `YGLWindow`; GLFW hidden window + GLEW); new Block 25 hand-builds a cube, allocates 256×256 RGBA8 + depth FBO via existing `Framebuffer`, renders via D-028's PBR shader path twice at roughness ∈ {0.1, 0.9}, glReadPixels each, asserts max per-channel byte diff > 30. SKIP-safe on GL-init / shader-load failure. Bug-probe (b) — comment out roughness uniform setter — is load-bearing. Also folds in turn-26 WARNING fixes (D-031 prose error + README placeholder line) and D-033: production `bottomUpHybridDepth` raised 3 → 30 (effectively pure GPU) based on D-031's chart. Parallel-symbol rule respected: new `HiddenGLContext` + Block 25 only; `YGLWindow`, `MeshGL`, `Framebuffer`, `Program`, shader files, Blocks 1–24 untouched. Self-test count 44 → 45. BDD-005 row promotes `warning → pass`.

- **2026-05-12, BVH refit benchmarking harness slice planned (this plan).** `feat/bvh-refit-bench` branched off `main` at `febc19b`. Closes Estimator turn 25 WARNING (D-030 default depth unmeasured). Design call (D-031): new `--bench-bvh-refit` CLI flag triggers a 4-method × 4-resolution × 10-frame sweep on a cloth-only FastGridCloth scene; methods map to `bottomUpHybridDepth` values {0, 1, 2, 30}; per-frame timing via existing `broad_refit` FrameProfiler scope; output in `profiles/experiment/bvh-refit-2026-05-12/` (CSV + Korean README + Python chart script + 2 PNG charts). Block 24 smoke test in `runSelfTest` (1×1×1 micro-config; verifies CSV mechanism, NOT perf numbers). Parallel-symbol rule preserved: bench code is all NEW symbols (`runRefitBench`, `BenchConfig`, `BVHRefitMethod` enum, argv branch); production refit / hybrid / boxes paths untouched. "Full GPU" routes through `bottomUpBoxesPartialGPU(30)` not strict `bottomUpBoxesGPU` (overhead delta is sub-noise; documented). User explicitly deferred a source-file split refactor (tests-in-main convention preserved for now). Self-test count 43 → 44. No new BDD/FR.

- **2026-05-11, BVH bottom-up GPU combine slice planned (this plan).** `feat/bvh-bottomup-gpu` branched off `main` at `eb1413e`. Surprising investigation finding: the user's algorithm sketch (atomic-fetch-add halving + memory fences for lock-free bottom-up AABB combine) is **already in the codebase** as `src/metal/bvh.metal::bottomUpBoxes` (line 394), but with `memory_order_relaxed` and no fences — the prior author self-flagged it with `// this is problematic. No guarantee for the saving childA, B` and parked it. The current production runs CPU `bottomUpCombine` at `src/main.cpp:3730`. Design call (D-029): fix `bottomUpBoxes`'s memory ordering (acq_rel atomic + release/acquire fences with `memory_scope_device`) and wire it as production for both build and refit. Reuse existing `treeParent` array (no `BVHNode` struct change). Repurpose existing `bottomUpReadyA` buffer field → `treeVisitCounts`; drop `bottomUpReadyB` and `bottomUpProgress`. Retire the multi-pass `bottomUpCombineStep` + `initBottomUpReady` + `clearBottomUpProgress` kernels + `bottomUpCombineGPU` C++ driver in the same slice. CPU `bottomUpCombine` stays as Block 21's correctness reference. Block 21 mechanizes per-interior-node bit-equality between GPU and CPU paths (stricter than root-only — catches localized race conditions per PLANNER.md step 7). `enlargeTrajectory` (velocity-inflated leaves) stays CPU for this slice; future GPU port. Apple Silicon's strong memory model may silently rescue spec-incorrect atomics; bug-probe strategy includes "remove release fence" and "switch to relaxed" probes but documented as best-effort observability with the spec-correctness invariant in D-029 regardless. New D-029 + likely CM-011.
- **2026-05-07, harness slice fix turn.** Estimator BLOCKed: `BDD-009`/`011`/`012` self-test bodies pattern-matched the matrix labels rather than mechanising the spec wording (no wind in BDD-009; no runtime-without-restart pivot in BDD-011; no actual wind application in BDD-012). Plus Metal-less host (Codex container) made `verify.sh` exit non-zero before any assertion ran. Fix plan: rewrite the three blocks against `docs/TESTS.md` "Then" clauses verbatim (full state.x/state.v strict equality for Float; gravity-runtime-pivot without re-init for BDD-011; wind-drives-cloth-+x for BDD-012); flip the null-device path from FAIL to SKIP so the Estimator's host is supported. Pattern lesson: harness assertions must be authored from `docs/TESTS.md`, not from the compressed matrix-row labels.
- **2026-05-07, harness slice second turn (commit `a0b5fca`).** All eight assertion blocks pass on macOS Apple Silicon; Estimator's Linux container takes the SKIP path and `verify.sh` exits 0 cleanly. Slice merged into main (FF-merge after commit). Test matrix rows BDD-009/011/012/015 now `pass` with addresses pointing at the rewritten block names.

## What the Estimator should know

- The backend-boundary invariant (`BDD-103`) applies to *every* slice. Any persistence-slice change that touches `src/shader/` or simulation kernels is suspect.
- "Determinism, scoped" (`BDD-102`) is a *single-machine* promise. Don't escalate cross-machine drift to BLOCK in v1.
- Reserved-but-not-shipped behaviors (`Elastic`, `Fluid`, `Generator`) keep their enum identifiers — reordering them silently corrupts saved scenes.
- `GeneralMesh::rotationQuat` exists as of D-007 but no consumer reads it yet. A future render/sim slice that *adds* a consumer is implementing FR-004, not violating BDD-103 — it's reading a field that's already there.
- The persistence slice's WARNING about app-level coverage (`Simulator::saveScene/loadScene` unexercised end-to-end) is parked until the test-harness slice resolves Q-D. The matrix lists BDD-015 as `pass` because the JSON-layer round-trip is the testable subset today; calling it `pass` is honest under "what we can verify with the harness we have", not under "every claim in TESTS.md#BDD-015 is mechanically checked".
- For the **environment forces slice** the same partial-pass convention applies: gravity/wind round-trip and Environment-panel wiring are unit-testable, but the "object accumulates velocity in +x" clause from BDD-011 (and the strict-equality clause from BDD-009) require sim-step execution and are parked behind the test-harness slice. Expect a `WARNING` row, not a `BLOCK` — this is the documented gap, not new debt.
- **BDD-005 render clause is manual-test-only** (introduced by D-028, the PBR preview shader slice). The harness has no GL context — verifying "preview render reflects the lower roughness" requires either an offscreen FBO harness (separate slice candidate) or visual user gate. Matrix row stays `warning` until the FBO render harness ships. **Standing structural WARNING** — future Estimator turns should NOT re-flag this as new debt, parallel to BDD-102's Alembic-bytes substitution.
